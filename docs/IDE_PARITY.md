# IDE parity roadmap

## Product position

Codevo Editor is a focused desktop IDE with a broad PHP and JavaScript/TypeScript core. It is not yet a 1:1 replacement for VS Code or PhpStorm. In particular, VS Code's extension ecosystem and several bundled PhpStorm tool windows are platform-sized capabilities, not single checklist items.

Parity claims must therefore be made per workflow and backed by automated acceptance tests.

## Strong today

- Workbench: multi-tab/split editing, workspace persistence, search/navigation, command palette/keymap, Problems, terminal, settings and Git workflows.
- Language platform: isolated per-workspace PHP and TypeScript language-server sessions, document sync, diagnostics, completion, navigation, symbols, references, rename, code actions, formatting, semantic tokens, inlay hints and hierarchies.
- TypeScript/Node: managed TypeScript server, workspace TypeScript selection, auto-import preferences, ESLint, Prettier, package scripts, strict project-scoped launch configurations, a workspace JavaScript Test Explorer, workspace-wide static Express route navigation, Jest/Vitest execution and CDP debugging with source maps.
- PHP: PHPactor/Intelephense, Composer/PSR-4 indexing, PHPStan, Pint, PHPUnit/Pest workflows with Clover coverage and active-editor gutters, Xdebug/DBGP, refactors and Laravel/Nette/Blade/Latte/NEON intelligence.
- VCS: file/hunk staging and revert, commits, push, history, blame, branches, remotes, stash, cherry-pick and nested repositories.

## Material gaps

| Priority  | Workflow gap           | First vertical slice                                              |
| --------- | ---------------------- | ----------------------------------------------------------------- |
| P1        | Git collaboration      | Fetch/pull/merge/rebase and conflict-resolution workflow          |
| P2        | Database/SQL tools     | Connections, schema browser and query console                     |
| P2        | Docker/remote runtimes | Remote Node/PHP interpreters, deployment and debugging            |
| P2        | HTTP client            | Project request files, environments and response history          |
| Strategic | Extension platform     | Decide a stable plugin API/security model before marketplace work |

## Delivery rules

1. Implement small end-to-end slices with domain tests, workspace/session stale-result guards and UI acceptance tests.
2. Keep domain code independent of application, components and infrastructure.
3. Preserve exact Tauri wire contracts and validate untyped payloads at the boundary.
4. Do not label a workflow complete until its common happy path, failure path and multi-project isolation are automated.
5. Track extension-provided VS Code behavior separately from editor-core behavior.

## Recently completed parity slices

The workspace-wide Express Routes foundation is complete. Bounded workspace discovery feeds a dependency-free parser for literal route methods and chained `route()` declarations. Results have deterministic identities and ordering, replace disk routes for dirty files instead of duplicating them, refresh from watcher invalidations, remain isolated across workspace switches and expose searchable keyboard-accessible navigation to the exact file and source position. Dynamic route paths remain intentionally omitted.

The JavaScript Test Explorer slice is complete. It discovers Jest/Vitest tests into a workspace/file/suite/test tree, supports all/file/suite/test runs, maps parameterized runtime results conservatively, retains completed statuses per workspace and invalidates discovery on relevant file-watcher events without running tests merely because the panel opened. Test execution remains trust-gated; discovery, report parsing and source reads are bounded, and stale workspace work cannot overwrite the active workspace.

The project-scoped Node launch slice is complete. The Settings UI provides accessible list/select/create/edit/delete/default management for strict `.codevo/launch.json` script, Jest/Vitest and npm targets, including validated `args`, workspace-relative `cwd` and `env`. Explicit saves use revision-aware owner-scoped writes and atomic no-overwrite creation; conflicts preserve the draft, workspace trust gates mutations, and workspace switches cannot publish stale reads or writes.

Npm debugging executes no shell. It accepts only direct `node <workspace-file>` commands and the conservative workspace-local `tsx`, `ts-node` and `ts-node-esm` wrapper allowlist; shell syntax, unknown wrappers, paths outside the registered workspace, unsafe environment overrides and over-budget inputs fail closed. Wrapper inspector discovery accepts one bounded `127.0.0.1` endpoint, rejects ambiguous endpoints, redacts inspector tokens from output and retains the existing trust, captured-owner and stale-start guards.

The advanced Express intelligence slice now resolves literal `app.use('/prefix', router)` mount chains across local routers and unambiguous relative ESM/CommonJS imports. It recognizes selected Express factory, `Router` factory, application and router aliases, composes nested and multiply mounted runtime paths deterministically, and immediately re-resolves dirty cross-file overlays. Dynamic prefixes, unresolved or ambiguous imports, cyclic-only graphs and non-relative package routers do not produce speculative prefixed paths; their existing router-local declarations remain navigable.

The package-management vertical slice is complete. The Packages panel presents the active JavaScript/TypeScript workspace's declared production and development dependencies, detected npm/pnpm/yarn/bun manager, declared ranges and installed/missing state. It is searchable and keyboard-accessible, navigates to the exact dependency key in `package.json`, prefers an unsaved manifest overlay and rejects stale workspace reads.

Install, update, remove and outdated actions are trust-gated and require an explicit manager/argument preview followed by confirmation. The typed IPC boundary rejects malformed or oversized values; the Rust side re-derives every command from an allowlist, uses no shell, serializes mutations, executes from the retained workspace root, bounds time/output and kills the process group on timeout. Unsaved `package.json` changes block execution. Modern Yarn exposes no core non-interactive read-only outdated command, so that one combination reports an explicit unavailable result rather than running a speculative command.

The first Symfony vertical slice is complete. Exact Composer detection activates a typed Commands/Routes/Services panel with filtering, keyboard navigation, explicit refresh and conservative PHP controller/service navigation. Inspection runs only for a registered trusted workspace, without a shell, with retained-root identity checks, bounded time/output/results, process-group termination and per-workspace/global concurrency limits. Watcher changes invalidate relevant Symfony data without leaking work across projects; unavailable, malformed and stale responses remain explicit and tested.

The first Nette operational slice is complete. Exact `nette/application` detection activates a searchable keyboard-accessible Services panel without misclassifying standalone Latte projects. A bounded pure projector reuses the canonical path-preserving NEON loader and its include/merge precedence, resolves aliases conservatively, applies unsaved NEON overlays to both content and include graphs, and navigates to exact definitions or resolved PHP classes. Watcher invalidation, workspace-owner/request-sequence guards, tagged failures and deterministic caps keep results isolated and explicit.

The Nette Presenters slice is also complete. The same Nette panel now lazily switches between Services and a keyboard-accessible presenter treegrid. Existing bounded presenter/template discovery feeds a pure projector for presenter classes, merged `action*`/`render*` actions, `handle*` signals and only existing modern/classic Latte templates, all with exact source anchors. Dirty PHP/Latte overlays are debounced and serialized; only the latest queued refresh runs, and stale workspace/navigation work cannot commit.

The Nette Routes slice now covers both legacy `new Route(...)` declarations and statically proven modern `RouteList::addRoute(...)` registration. A bounded parser tracks only confirmed FQCN/import-alias RouteList receivers, PHP namespace and case rules, reassignment/reachability taint, constructor and chained `withModule()` prefixes, flat literal targets and positional/named `oneWay`; unrelated classes, nested metadata, malformed calls and dynamic/control-flow ambiguity remain omitted. The existing lazy grid, dirty overlays, exact navigation, ordering, caps and latest-only refresh consume the richer projection without a new backend surface.

NEON references and rename now operate across a fresh bounded include graph. The repository snapshot includes outgoing and reverse edges, dirty open-model overlays, Windows/UNC-safe identity and explicit incomplete reasons; uniquely declared services and dotted parameters resolve through sibling, reverse and cyclic components while aliases, setup and service-factory references participate without treating class/generated/include targets as symbols. Workspace rename uses a root/trust/owner/generation-fenced single-flight transaction with exact open-model content/version CAS, closed-file hashes, atomic finalization and concurrent-edit-safe rollback. Incomplete, ambiguous, stale, forged or partially applicable plans fail closed with no local workspace fallback; scratch documents retain the conservative same-file behavior.

Nette NEON semantic diagnostics are also live. A composable immutable rule pipeline reports unresolved parameters/services, same-document duplicate declarations and provable alias cycles from the same fresh snapshot, and publishes exact owner-scoped Monaco markers only for complete current results. Dirty overlays, disconnected open components, model/root generations, cancellation, result caps and close/switch/unmount cleanup are bounded and tested; incomplete/truncated snapshots authoritatively clear rather than inventing partial warnings.

Native directory enumeration now stops at its validated entry cap instead of materializing an unbounded directory. Operational Nette reads additionally enforce directory, entry, per-file and aggregate byte budgets with fail-fast behavior. Services, Presenters, static RouteList routing, cross-file NEON references/rename and semantic diagnostics are complete. Arbitrary runtime PHP evaluation and dynamic route expressions remain intentionally unsupported.

The resumed JavaScript/TypeScript/Node coverage slice now includes one-click Jest/Vitest line coverage in the Test Explorer and active-editor gutter decorations. Coverage runs use fixed no-shell arguments, a private temporary report outside the workspace, trust and retained-root identity checks, bounded LCOV parsing and deterministic cleanup. The panel shows workspace and per-file line metrics and safely opens the first uncovered line; dirty documents, workspace switches, trust changes, clears and concurrent requests cannot publish stale decorations or results.

Monorepo package scripts now have a package-grouped, paginated Scripts sidebar over the typed cancellable task lifecycle. Discovery and execution remain bounded and no-shell; active tasks stay visible and stoppable across paging and discovery refreshes, terminal startup uses an explicit acknowledgement handshake, and legacy blocking execution is no longer exposed.

Node debugging now supports composable conditions, typed hit counts and logpoints from the editor gutter and Debug panel. The TypeScript and Rust boundaries are strict and bounded, PHP rejects unsupported behavior, and the CDP runtime evaluates logpoints through a response-driven state machine without successful-pause UI flicker. Explicit user pause, internal timeouts, late responses and evaluate/resume failures retain a truthful visible debugger state.

Typed monorepo package tasks now publish owner-tagged, sequence-ordered and bounded output independently of the raw terminal stream. Conservative TypeScript and ESLint problem matchers run over byte-safe stdout/stderr assemblers, produce navigable Problems snapshots and remain tied to retained workspace/package directory descriptors across renames and path replacement. Start acknowledgement, stop/trust/root cleanup and final snapshots prevent early or stale task events from leaking across owners.

Node Watch Expressions are persistent per workspace and evaluated automatically only while paused, with side-effect-safe CDP policy. Results are owned by the exact session, pause generation and selected frame; resume, step, replacement, trust loss and root changes invalidate pending values. PHP exposes an explicit unsupported state for automatic Watches while keeping its user-initiated Debug Console evaluation available.

The Debug Console now uses an application-owned chronological model for process output and pending/completed evaluations. Its bounded history and final session snapshot survive panel switches, while session/pause/request ownership rejects stale results and a single truncation marker truthfully represents retained output limits.

Paused trusted Node sessions now provide bounded Debug Console IntelliSense for lexical identifiers and statically safe member paths. Suggestions open automatically after a dot or a two-character prefix and explicitly through `Ctrl+Space`; Arrow keys, Enter/Tab, Escape, mouse selection, history navigation and caret movement have deterministic non-conflicting behavior in an accessible combobox/listbox. Accepting a suggestion replaces only the parser-owned UTF-16 range and never evaluates or submits the expression. At most 100 suggestions enter the DOM, while truthful incompleteness asks the user to keep typing.

The frontend sends only a tagged lexical/member query, never the raw REPL expression. Calls, optional chaining, dynamic keys, private fields, malformed Unicode and over-budget input fail closed. Latest-wins request and input revisions bind workspace owner, root, Node adapter, session, pause generation and selected frame through debounce, IPC and publication, including A→B→A replacement and cursor movement before the popup opens.

The strict `debug_completions` backend enumerates lexical shadowing and ordinary own data-property descriptors through CDP `Runtime.getProperties` only. It never evaluates an expression, calls a function or invokes getters/proxy traversal. Retained-root authority, trust, session, pause and frame are rechecked before, during and after CDP work; descriptor/request quotas, a full-pipeline deadline, 200-item/1,024-byte-label/64 KiB response caps and truthful `isIncomplete` prevent speculative fallback. A truncated nearer lexical scope blocks outer-scope fallback rather than guessing through shadowing. PHP remains unsupported.

The debugger Variables and expandable Watch-values slice is complete. Every reference belongs to the exact workspace, session, pause generation and selected frame; strict bounded page requests and serialized inspection permits reject stale work across resume, stop and replacement. CDP and DBGP use real bounded child paging, while the accessible shared tree provides owner-bound loading and retry, complete keyboard navigation, explicit terminal states and a hard 500-row DOM cap.

Selected Jest/Vitest debugging is now available directly from Test Explorer file, suite and test nodes. An exact typed TypeScript/Rust selection contract carries no user arguments, environment or working directory; execution is no-shell and confined to the captured monorepo package and registered workspace. Jest runs serially and Vitest exposes the inspector through a single fork worker, while trust, exact-owner and stale-start guards prevent cross-workspace publication.

Debugger startup and event delivery are now hardened as one lifecycle. Starts are physically serialized per root with latest-start-wins invalidation and a bounded global cap; disposal, trust loss and replacement fail closed. Pending and live delivery have count and byte limits, `Started` is delivered before buffered output, terminal state publishes exactly once, overflow has one bounded diagnostic, and sink failures discard queued state without invoking callbacks under registry locks.

Paused Node debugging now provides editor hover values for bounded JavaScript and TypeScript member expressions. Evaluation is side-effect-safe and owned by the exact workspace, session, pause generation, selected frame, model and document version. Latest-wins cancellation, a global concurrency cap, masked source extraction and bounded untrusted Markdown keep stale, executable and oversized results out, while the dedicated provider coexists with LSP hover content.

Paused Node inline values are now available on the selected frame's stopped JavaScript/TypeScript line. The editor loads only page zero of the first two distinct non-expensive root scopes (at most 200 variables), never evaluates expressions or expands nested references, and renders at most 12 cached values. Exact workspace/session/pause/frame ownership and trust checks reject stale data; dirty source, resume, frame/path/tab/model changes and source-version invalidation clear the non-growing decorations. Malformed Unicode, control characters, bidi formatting controls and Unicode line separators are neutralized before bounded display.

Named Node launch configurations can now be loaded, selected and started from the Debug panel. The controlled selector exposes only each validated name, default flag and target kind while complete arguments and environment stay in an application-owned map. Script, test, npm and loopback attach targets reuse the existing no-shell launch mapping; exact workspace identity, trust, session-idle state, request generation and read-port ownership are checked throughout load and start. The original active-document/default F5 selection remains intact and now applies the same stale/trust guards and bounded warning policy.

Jest and Vitest failures now publish truthful, navigable Problems through an owner-scoped result ledger. Successful all/file/suite/test runs replace only the scope they authoritatively cover; failed, unavailable, stale and superseded runs preserve known results, while explicit invalidation advances the owner generation even when Test Explorer is closed. Projection and IPC text are bounded consistently, unsafe paths and display text fail closed, overflow remains explicit, and manual Clear stays dismissed for the exact snapshot across workspace switches until a new run identity arrives.

Node Debug now provides toolbar-first Restart for the exact active owned session. The application retains an immutable validated launch target privately, stops the matching session, rechecks the exact workspace root, session ownership and trust boundary, then replays that retained target through the existing launch path. Restart is single-flight, keeps arguments and environment secrets out of UI state and errors, and performs bounded orphan cleanup when stop or replay crosses a stale lifecycle boundary.

The same lifecycle is now available from the command palette as `debug.restart` / **Debug: Restart**, with `Shift+Cmd+F5` on macOS and `Shift+Ctrl+F5` on Windows and Linux. The command is enabled only for a trusted workspace with the exact owned running or stopped Node session and its retained restart target, and remains disabled while start, restart or active-stop work is in flight. Command state exposes no launch arguments or environment values.

Named Node launch configurations are now also available from the command palette through **Debug: Select and Start Configuration**. Its searchable keyboard-first picker exposes only bounded safe metadata while complete arguments and environment values remain in the private application-owned target map. A launch occurs only after an explicitly accepted selection, and exact workspace-owner, trust and read-port identity are revalidated before dispatch; lifecycle or focus changes fail closed instead of starting a stale target.

Bulk breakpoint management is complete in both the Breakpoints toolbar and command palette through **Enable All Breakpoints**, **Disable All Breakpoints** and **Remove All Breakpoints** (`debug.enableAllBreakpoints`, `debug.disableAllBreakpoints` and `debug.removeAllBreakpoints`). Actions are count-aware and single-flight, update the exact persisted workspace model, and synchronize the precise old/new file union to a live session with at most four concurrent requests. Exact workspace owner, trust, root, session and synchronization-generation guards stop queued or stale work, while a later per-file edit always wins over an older bulk response; no backend lifecycle expansion was required.

Node Debug now supports `debug.runToCursor` / **Debug: Run to Cursor** with `Ctrl+F10` on macOS, Windows and Linux. The action is exposed only for the exact trusted, owned and paused Node session with a clean writable workspace-confined JavaScript/TypeScript document. It captures and rechecks the workspace, session, pause generation, document identity, revision, saved content, cursor line and column, and trust boundary before dispatch, so dirty documents, declaration files, PHP sessions and stale focus fail closed.

Run to Cursor maps the requested source line and column through the active source map, resolves one ephemeral CDP breakpoint and removes that exact id before continuing. It never adds to, removes from or rewrites the persistent breakpoint model. Missing resolution or stale ownership aborts the action; temporary cleanup is retried once, and a second cleanup failure terminates the debug session rather than leaving a hidden breakpoint behind. Independent adversarial reviews covered command and UI guards, concurrency, stale lifecycle transitions, strict TypeScript/Rust IPC validation, source confinement, source-map coordinates, resolution races and fail-closed cleanup with no unresolved critical/high finding.

Debugger stepping now matches the VS Code defaults for **Debug: Step Into** on `F11` and **Debug: Step Out** on `Shift+F11`; fresh bookmark bindings moved to `Alt+F11` and `Shift+Alt+F11`. Continue, Step Over, Step Into and Step Out share one exact trusted, owned, stopped-session gate and remain disabled while debugger mutation work is pending.

Shortcut collisions now use a deterministic reverse Chain of Responsibility: the newest enabled matching command wins, an unavailable candidate falls through to an older enabled binding, and a known all-disabled shortcut is still consumed safely. The same chain is used by Monaco's editor-focused `F12` dispatch, preserving Go to Definition consistency while keeping legacy and custom bindings as deterministic fallbacks.

Debug Console Focus and Clear are now complete. **Debug: Focus Console** uses the VS Code platform defaults `Shift+Cmd+Y` on macOS and `Ctrl+Shift+Y` on Windows and Linux; its frontend-only, owner-safe focus generation opens the Debug bottom-panel view and deterministically serves both repeated requests and delayed input mounts without IPC. **Debug: Clear Console** and the accessible Console-header button share the same owner-safe clear operation: an empty console is a disabled no-op, evaluation history remains intact, pre-clear pending work cannot repopulate cleared entries when it settles late, and future owned output appears normally.

Node **Run Without Debugging** is now available on the VS Code `Ctrl+F5` default. Config-first resolution reuses validated `.codevo/launch.json` script, Jest/Vitest and npm targets before conservative plain active-script or active-test fallbacks; attach, PHP, selection-only, dirty and inspector-enabled targets are rejected. Execution is a typed Node terminal task, not a debugger session: three strict commands provide start, acknowledgement and stop, and one bounded owner-tagged status event reports its lifecycle while existing terminal output delivery remains the sole output path. Node runs share package-task admission for the same workspace/terminal session, and exact trust, retained workspace, terminal, run-id and generation checks close resolution/start/acknowledgement/event/stop races. Inspector endpoints are never created, and arguments or environment secrets are not exposed through UI or status events.

The run lifecycle now has an explicit **Run: Stop Without Debugging** command and accessible Status Bar Node chip. Pre-start resolution and terminal acquisition cancel locally; starting or running work stops only its exact owner. A dedicated `stopping` state suppresses duplicate requests and late `running` events, while a rejected stop becomes explicitly retryable from both surfaces without regressing the lifecycle. Labels are bounded and sanitized, expose no target, arguments, environment or raw failure detail, and reuse the existing stop command/status event without new IPC.

Named configurations are now available through **Run: Select and Start Without Debugging**. Run and Debug reuse one private-target picker engine through separate Strategy adapters and one coordinator scoped to the workbench instance, preventing simultaneous focus traps while preserving the existing Debug picker. Public launcher, command and UI state contains only bounded names and safe target metadata; arguments, environment, working directories and resolved paths remain in application-owned refs. Attach, PHP and inspector-enabled configurations are excluded, and exact workspace identity, trust, retained read-port ownership and path containment are revalidated before the existing Node-run lifecycle accepts a start. This frontend-only slice adds no IPC or Rust surface.

Run and Debug toolbar discoverability now includes a secondary **Run: Select and Start Without Debugging** button backed by a narrow Command adapter. It is shown only in JavaScript/TypeScript workspaces, does not require an active document, rechecks live availability before mutation and only opens the existing picker without implicitly starting anything. The toolbar receives only `canOpenPicker` and `openPicker`, keeping private choices, paths, arguments, environment and working directories out of its state and DOM. It reuses the same instance-scoped Run/Debug coordinator and adds no IPC or Rust work. An independent adversarial audit covering gate drift, focus coordination, accessibility and Debug-panel regressions completed without a P1/P2 finding.

`debug.configureNodeLaunchConfigurations` / **Run: Configure Node Launch Configurations** is now available from the command palette and an accessible Run and Debug toolbar gear. Both routes open one controlled, revision-aware configuration dialog after closing either Run or Debug configuration picker; opening it never writes a configuration or starts a target. The dialog is bound to the exact workspace owner and JavaScript/TypeScript capability and fails closed across either drift, remains read-only in untrusted workspaces, traps focus, closes on Escape and restores focus to its opener. This frontend-only slice adds no IPC or Rust work, and an independent adversarial audit found and fixed two P2 issues.

`debug.addToWatchAtCursor` / **Debug: Add to Watch** now derives a bounded JavaScript or TypeScript member expression from the exact live Monaco model, URI-bound model identity, model version, dirty content, cursor and scoped editor/workspace owner. It reuses the side-effect-safe hover parser, accepts the conventional caret immediately after a token and rejects calls, bracket access, private members, masked source and stale double reads. Adding a definition requires neither trust nor a paused session and never evaluates directly; the existing trusted, stopped Node Watch lifecycle remains the only automatic evaluator.

The Watch reducer now owns an atomic boolean add decision over its exact count, duplicate and serialized-payload limits, so synchronous duplicate requests cannot report success or open the Debug panel without a real state transition. Independent adversarial review also fixed editor-owner mismatch handling and verified A→B→A, same-root replacement, split focus, persistence rollback and stale command closures. The command has no default shortcut and adds no IPC or Rust surface; source content and expressions remain outside public UI and command state.

The official menu-only `debug.addToWatchExpressions` / **Add to Watch** action is also available on Node Variables value rows. It accepts only the adapter-provided `evaluateName`, never reconstructs an expression from labels and never evaluates directly. The private one-shot capability binds the exact workspace, session, pause, frame, inspection generation, row epoch and identity before delegating to the existing atomic Watch add operation; scopes, status rows, load-more rows, PHP and stale or reordered Variables fail closed. Duplicate, full and oversized Watch collections remain authoritative no-ops. The command is hidden from Command Palette, has no default shortcut and adds no IPC.

The official `testing.runAtCursor` and `testing.runCurrentFile` commands now use the Test Explorer lifecycle rather than the legacy terminal-based command path. Their VS Code chords are `Cmd+; C` and `Cmd+; F` on macOS, mapped to `Ctrl+; C` and `Ctrl+; F` on Windows/Linux, and are active only under `editorTextFocus`. A bounded exact Monaco capture, clean saved-file disk parity, workspace owner, trust and activation fences produce a typed test/file scope; a private bridge then delegates to the mounted Explorer's existing discovery, run, result, problem and coverage invalidation lifecycle. Invalid scopes, concurrent dispatch, unmount, stale A→B→A ownership and late results fail closed without exposing source or runner internals through the public workbench graph. Independent audit additionally monotonic-fenced trust, workspace-root and workspace-ID A→B→A transitions and restored the official shortcut metadata at the command-registry boundary; no P0/P1 or remaining P2/P3 issue was found.

`editor.debug.action.selectionToRepl` / **Debug: Evaluate in Console** now matches the official VS Code selection/current-line behavior. A synchronous live capture binds the exact Monaco model, selection range, document and editor/workspace owner; selected text is used whenever the selection is geometrically non-empty, while only an actually empty selection falls back to the current line. Non-empty whitespace and multiline/control-bearing input fail closed through the existing typed expression contract.

The action is gated to the exact trusted, owned and stopped Node session, pause generation and selected frame. It requires matching double captures before the existing Debug Console `submit` side effect, remains single-flight, and reuses owner-safe console focus without adding IPC or Rust. Adversarial audit fixed retry handling after a synchronous focus throw and the whitespace-selection fallback, then hardened top-frame ownership plus synchronous and asynchronous submit failure handling.

The paired official **Debug: Go to Next Breakpoint** and **Debug: Go to Previous Breakpoint** commands are complete under their VS Code IDs. They navigate enabled breakpoints only: first by a strict later/earlier line in the current file, then by canonical file-path lexicographic order, and finally by wrap. Current-line entries are skipped except for the official single-breakpoint self-wrap case.

A minimal live reader captures the exact focused JavaScript/TypeScript model, version, canonical document/root owner and current line. Selection operates on a private immutable breakpoint snapshot, while the owner-safe navigation port receives a commit fence that requires the target to remain enabled at the same canonical path and line. Navigation is globally single-flight across both directions and requires no trust, debug session, shortcut, IPC or Rust change. Adversarial audit fixed concurrent direction dispatch, synchronous/asynchronous port throws and workspace-path aliases without exposing conditions, hit counts or log messages.

The official `debug.copyStackTrace` / **Debug: Copy Call Stack** action is complete in the command palette and accessible Call Stack toolbar. Its pinned pure formatter preserves frame order and full filesystem paths, emits `name (fsPath:1-based-line)` without a column, emits only the frame name when no file exists, joins with deterministic LF and fails closed for empty, malformed or more than 1,000 frames without mutation.

Clipboard access uses a domain port consumed by the application coordinator and an explicit browser adapter injected from App, preserving dependency direction and adding no IPC. Generic owned Node and PHP stopped pauses are supported without a trust requirement. Exact root, session, pause generation, workspace owner and the complete frame snapshot must match across two deep captures, including same-root A→B→A replacement; one global flight permits exactly one write and becomes retryable after synchronous or asynchronous failure.

The command has no shortcut, and public command/toolbar state contains no frames, arguments, environment, variables, conditions or log messages. Adversarial audit fixed stale inspection-owner acceptance, throwing frame access and dependency-direction violations while keeping manual debugging unchanged.

The official `editor.debug.action.toggleInlineBreakpoint` / **Debug: Inline Breakpoint** action is complete on `Shift+F9`. It follows VS Code's add-only semantics: an exact duplicate is a no-op, cursor column 1 creates a line breakpoint and later columns create distinct inline siblings. Ordinary `F9` remains line-only. Breakpoint identity, persistence, navigation, rollback and Monaco decorations preserve the exact absent-versus-present column discriminator, including an explicit inline column 1 received from an adapter.

The live capture is fenced by model, version, workspace/editor owner and a monotonic focus epoch, including A→B→A and permanent epoch-exhaustion fail-closed handling. Decoration relocation writes back by exact breakpoint ID and rejects stale models, ownership drift and collisions without feedback loops. The existing typed `debug_set_breakpoints` IPC now carries an optional strict non-zero `u32` column; Node/CDP maps it bidirectionally through raw and source-mapped immediate, early and late resolution paths, while PHP/DBGp rejects inline locations explicitly. Failed CDP removal terminates the adapter before any re-arm, preventing ghost breakpoints. Independent adversarial audits fixed epoch-exhaustion republishing and found no remaining defect.

The four official Call Stack navigation commands are complete under `workbench.action.debug.callStackTop`, `callStackBottom`, `callStackUp` and `callStackDown`, with their official titles and no default shortcuts. A pure immutable selector preserves adapter order, wraps Up/Down, accepts fileless frames and fails closed for empty stacks, unknown selections and invalid or duplicate frame IDs. The application adapter reuses existing frame selection with exact snapshot, session, pause-generation, root, owner, selection and target fences plus one shared flight across all directions; no IPC or Rust surface was added.

Call Stack rows now expose one roving tab stop, `aria-current`, Arrow Up/Down and Home/End focus movement without changing the selected frame. External selection and frame removal resynchronize focus to the selected, top or first visible frame, while a fileless frame remains selectable without attempting source navigation. Two adversarial audits fixed selection/rerender and removed-frame focus drift, then verified command metadata, live enablement, mixed-command races, failures and public-state privacy.

The official `workbench.action.debug.restartFrame` / **Restart Frame** action is complete as a Node-only selected-frame Call Stack action. It has no shortcut and is deliberately callable from the registry but absent from Command Palette, matching VS Code. The accessible inline action is shown only for the current restartable Node frame, excludes label/subtle frames, rechecks live capability on click and does not expose frame data through its public port.

The typed request carries only the exact workspace root, session, pause generation and frame ID. Application ownership and the shared debug-control flight remain locked across the CDP ACK until a fresh pause or termination; an accepted ACK immediately invalidates old frame, selection, scope and variable authority. CDP maps the logical frame ID to its current `callFrameId`, invokes `Debugger.restartFrame` with `mode: StepInto`, and marks only the resulting fresh pause with reason `restart`. Its marker is bounded and cleared on errors, disconnects and timeout, so later unrelated pauses cannot be mislabeled. PHP/DBGp rejects the capability explicitly.

Independent adversarial audits fixed an unbounded backend marker, delayed stale-pause invalidation, pending/trust UI propagation, duplicate-frame actions and direct-command presentation-hint bypasses. Restart lifecycle and CDP code were extracted into focused modules, keeping both `useDebugSession.ts` and `debug_cdp_transport.rs` below the new-production structural limit without exceptions.

The official JavaScript/TypeScript import actions are complete: `editor.action.organizeImports` on `Shift+Alt+O`, plus the language-specific `typescript.*` / `javascript.*` **Sort Imports** and **Remove Unused Imports** commands. They reuse the existing language-server code-action boundary but apply only one exact current-file text edit set under workspace, session, model, content, sync-version, trust and read-only fences. Disabled, stale, command-bearing, cross-file, file-operation, overlapping and path-alias-ambiguous actions fail closed; preferred actions are selected deterministically. Native Windows, UNC and POSIX identities remain distinct and traversal-safe.

`testing.debugAtCursor` / **Debug Test at Cursor** is complete for Jest and Vitest. A bounded immutable resolver selects the deepest containing test or nearest preceding declaration, rejects ambiguous exact/prefix filters and preserves parameterized matching. The application coordinator uses the exact saved editor capture, verifies the disk file byte-for-byte, bounds runner discovery by read count, per-file bytes and aggregate bytes, and reuses the same owner-safe launch builder as Test Explorer. Workspace activation, trust, debug admission, A→B→A, concurrent requests and post-accept lifecycle are fenced. Its official editor-scoped chord is live as `Cmd+; Cmd+C` on macOS and `Ctrl+; Ctrl+C` on Windows/Linux under exact `editorTextFocus` ownership.

The keymap now has one shared immutable one- or two-stroke sequence model across parsing, defaults, conflict detection, Settings capture and Monaco registration. Settings records a complete chord instead of persisting a partial prefix, calls `stopPropagation()` for captured key events so they cannot reach workbench commands, and accepts a bare `F` as stroke one while still applying the existing printable-key modifier policy to completed shortcuts. Custom `Cmd` chords map platform-aware to `Ctrl` on Windows/Linux; the Settings conflict projection normalizes every persisted binding for the resolved platform, so mixed custom `Cmd`/`Ctrl` spellings are reported as the same non-mac conflict. Exact-plus-prefix conflicts remain visible to deterministic conflict resolution, and Monaco receives its native chord encoding. The workbench dispatcher waits at most two seconds for stroke two, preserves deterministic command-collision priority and resets on a wrong stroke, repeated key, Escape, blur, editor replacement, keymap replacement, timeout or unmount. Exact Monaco text-input ownership prevents an editor-only prefix from starting in widgets or another workbench input, and observed keymap/editor identity effects fence A→B→A replacement so the captured editor must still own stroke two.

The official npm tree `npm.openScript` workflow is complete. Script click, Enter and Space open the exact quoted key in the owning root or monorepo `package.json`; Run and Stop remain explicit buttons. A bounded JSONC locator supports comments, trailing commas, escaped keys and CR/LF/CRLF positions while rejecting malformed, duplicate, non-string and over-budget manifests. Dirty or clean open manifests are authoritative; disk reads use an exact-workspace-id bounded lease invalidated by owner, descriptor or watcher changes. Discovery remains available in untrusted JavaScript/TypeScript workspaces, while execution and every task/output/problem subscription stay trust-gated.

The official editor-context `npm.runSelectedScript` / **Run Script** workflow is complete. It is available only from a local writable `package.json`, is hidden from Command Palette, has no default shortcut but remains user-bindable, and uses VS Code's `navigation@+1` context-menu placement. A bounded JSONC reverse locator reads the exact live dirty buffer at `selection.anchor`, then reconciles the selected script with the current canonical discovery snapshot before reusing the existing typed package-task lifecycle. Immutable scalar captures, strict workspace descriptors, monotonic authority/discovery generations, live task admission and A→B→A fences reject stale, ambiguous, spoofed, cross-manifest, traversal, Windows/UNC alias and concurrent requests without fabricating scripts.

The official `workbench.action.debug.disconnect` / **Debug: Disconnect** action is complete for an exact accepted Node attach session. It intentionally shares `Shift+F5` with **Debug: Stop**: live command context selects Disconnect only for attach ownership and Stop only for launched ownership, while disabled and custom shortcut collisions remain deterministic. The Run and Debug toolbar switches to an accessible attach-only Disconnect action with a dedicated icon, label, title, busy state and retry-safe error path; cleanup remains available after workspace trust is revoked.

Disconnect uses the strict `debug_disconnect` boundary with an exact `{ rootPath, sessionId }` request. Frontend ownership also binds the accepted launch mode and workspace owner, shares one control/stop flight, retries after failure and rejects same-root owner drift, event-before-start-resolution, stale terminal events and late completion from a replaced session without clearing the replacement. The backend independently requires the exact registered attach mode, removes the session once and closes only its debugger transport. Attach startup preserves DAP-style continue semantics by completing `Runtime.runIfWaitingForDebugger` and `Debugger.resume`; disconnect never owns or terminates the external process. Real spawned-Node acceptance proves the program continues after transport close and can be reattached and disconnected again, while registry/event coverage proves exactly-once terminal publication.

Disconnect authority is now derived from the workspace registry's retained file descriptor rather than reopening the submitted path. Disposal therefore survives a renamed or temporarily missing root, while alias retargeting and re-registration produce a different authority and cannot disconnect the retained session. Hostile roots fail validation before registry lookup, exact authority/session matching remains mandatory and concurrent finish/disconnect still publishes one terminal event.

The official `workbench.debug.viewlet.action.copyValue` / **Copy Value** slice is complete for Node Variables and Watch values. The command is registered but hidden from the Command Palette and unbound by default. A focused real value treeitem owns local `Ctrl+C`/`Cmd+C`; inputs, edit controls, text selections, scopes, status rows and load-more actions keep native behavior. Right-click first selects the exact value target and opens the reusable accessible **Copy Value** context menu with managed focus, Escape/outside dismissal and owner-safe menu execution. Watch roots are eligible only for an enabled, current successful result; pending, error, disabled and stale results fail closed.

The official `debug.copyEvaluatePath` / **Copy as Expression** slice is complete for Node Variables, nested Watch values and JavaScript/TypeScript debug hovers. Variables and nested Watch rows expose the adapter-provided `evaluateName`; Watch roots deliberately do not synthesize one. Hover actions are emitted only from a current successful evaluation carrying official adapter metadata. An application-owned one-shot provenance registry, a single allowlisted Monaco command and opaque bounded tokens reject forged, replayed, stale-model, stale-owner, A→B→A and disposed actions. Arbitrary Markdown remains non-executable. Tree and hover commands share one clipboard flight, while the App-local bridge publishes only four frozen command methods and keeps candidate setters, panel surfaces and the hover one-shot input outside the public workbench graph.

The official `debug.setVariable` / **Set Value** workflow is complete for writable Node Variables and writable nested Watch values. The exact focused row exposes the action through the ordered context menu, `F2` on Windows/Linux, `Enter` on macOS and double-click; read-only values, scopes, status/action rows and Hover do not acquire the capability. Watch roots never acquire `debug.setVariable`; their separate menu-only `debug.setWatchExpression` workflow is described below. The accessible inline editor uses Enter to commit, Escape or blur to cancel, suppresses unchanged requests, keeps one pending mutation, preserves the draft/focus on a current error and silently closes when the row or pause becomes stale. Variables and nested Watch trees remain valid roving `treeitem` structures without nesting an input inside a button.

Only an App-local ephemeral row capability reaches the command bridge. The public controller graph contains the frozen `canBeginEdit` / `beginEdit` pair and no variable reference, name, candidate factory or setter. Exact publication tokens, live row identity/value/mutation checks, owner changes, A→B→A focus, duplicate-name reordering, stale context menus and local/global shortcut bubbling fail closed. A row-bound application adapter revalidates workspace, trust, Node session, pause, frame and cached object identity before calling the private mutation port. Successful authoritative results reconcile the exact paged and legacy row, invalidate old/new/cyclic descendants and fence late parent or child loads while retaining a hard limit of 16 physically active requests.

Nested Watch assignment reuses that same capability rather than introducing a Watch-specific variable-mutation port. A current enabled successful Watch may expose writable descendants only while paused in a trusted Node session with exact workspace, definition revision, owner, pause and frame identity. Disabled, pending, running, PHP, untrusted, removed, reordered or stale Watch state removes both the mutation provider and focused command surface. Parent Watch event guards prevent nested focus, context-menu, click or key events from replacing the exact descendant target, and the aggregate Watch-root plus nested-value display remains capped at 500 treeitems.

The strict `debug_set_variable` backend accepts only the workspace/session/pause/frame/reference/name/value contract. Retained workspace authority, the registry mutation lease and trust-revocation ordering serialize teardown with mutation. Pause-owned typed provenance distinguishes writable lexical scope slots, writable own object data properties and transitive read-only values. Only ordinary Watch object results create an internal mutable root provenance; their wire result still omits `canSetValue`, while REPL, Clipboard, proxy, internal and non-object results remain read-only. The RHS is evaluated exactly once on the selected frame; scope writes use `Debugger.setVariableValue`, property writes use a fixed non-interpolated `Runtime.callFunctionOn`, and descriptors are revalidated before and after assignment. From RHS dispatch onward every unprovable outcome is explicitly indeterminate and never retried. Temporary object groups are released on every path, object results retain a fresh live handle, special CDP primitives preserve exact `CallArgument` semantics, returned names are bound to the request, per-pause inspection quotas survive cache invalidation and a final pause/frame check precedes success.

Object references carry bounded parent/property lineage. Assignment revokes only the replaced property's old descendant subtree, preserving the parent, unrelated Variables, other Watch roots and distinct aliases; assigned object references receive the new exact lineage. Ancestor cycles reuse a reference only under identical mutation provenance, so private, symbol, accessor, synthetic or read-only back-edges cannot regain a writable handle. A single shared UTF-8 byte predicate keeps empty, control and over-1,024-byte property names inspectable but read-only and prevents wire/capability drift. Real Node inspector tests cover a shadowed local, primitive and object assignments, Watch nested property mutation, post-release child inspection and subsequent unrelated scope access; DBGp remains explicitly unsupported.

Backend-provided `evaluateName` paths now cover safe bindings, member access, canonical numeric indices, JSON-escaped property names and narrowly valid `this.#private` access. Synthetic/internal values, unsafe text, missing parents and over-budget paths have no accessor, and the object-reference cache deduplicates and caps entries per pause. Variables and Watch share one owner-fenced clipboard coordinator and the existing typed evaluate boundary through the dedicated clipboard context; exact root, workspace owner, session, pause, frame, generation, epoch, focused identity and value survive repeated captures before the single clipboard write. Unmount, stale owner, A→B→A replacement and focus/menu races revoke the candidate, and one global flight prevents duplicate writes.

Node clipboard evaluation uses a bounded host-side CDP preview serializer rather than injected page JavaScript, preserving restrictive CSP behavior and avoiding getter execution. Depth, node, key, UTF-8 output, request and total-time budgets are enforced; proxy or overflow previews and unsupported serialization fall back to the already displayed bounded value. The dedicated CDP object group is released on success and every error/timeout path. The same clipboard adapter is shared with Call Stack copying, so App owns one clipboard implementation rather than parallel copies. PHP/DBGp deliberately reports clipboard evaluation as unsupported and **Copy Value** is unavailable there.

Watch-root Set Value Phase 2 is complete through the official menu-only `debug.setWatchExpression` / **Set Value** action. It has no default shortcut, is absent from Command Palette and remains separate from expression rename: `debug.renameWatchExpression`, F2/Enter and root double-click still edit the Watch expression text, while the context-menu action edits the evaluated value. Nested Watch rows continue to use `debug.setVariable` and its existing shortcuts; PHP/DBGp explicitly does not support Watch-root assignment.

The Node implementation remains fail-closed while supporting safe mutable lexical identifiers plus statically proven member, canonical numeric-index and JSON-string-index targets, including bounded nested chains such as `obj.nested.leaf`, `items[0]` and `obj["a-b"]`. The authoritative selected-frame receiver is also supported as a distinct static root, including `this.prop`, `this[0]`, `this["a-b"]` and `this.nested.leaf`; bare `this` is never assignable. Expressions are capped at 4 KiB, property names at 1,024 UTF-8 bytes and static traversal at eight segments, while numeric indices must be canonical JavaScript safe integers. The frontend carries only an opaque mutation token bound to the exact Watch definition revision, expression, workspace, session, pause generation and frame. Identifier targets resolve to their captured lexical scope slot. Static paths additionally prove exact scope shadowing and ordinary own data-property descriptors for every traversed segment, pin the final parent before RHS evaluation, then reuse the existing non-interpolated property mutation path. Fixed strict-identity CDP calls revalidate object lineage, including the exact captured `this` object, without property access, coercion, getters or proxy traps.

The RHS is evaluated exactly once on the selected call frame. Tokens are single-use, proof and descriptor work is budgeted per pause, and every consumed-token attempt invalidates related ephemeral authority even when it fails. Stale, forged, reused, owner-drifted, identity-changed or post-side-effect-unprovable operations are rejected without retry. A proven assignment triggers a global reevaluation of enabled Watches and invalidates the old Watch/variable-page cache so dependent expressions and structured results refresh from authoritative debugger state.

This is still narrower than VS Code's universal DAP `setExpression` surface. Dynamic computed keys, calls, optional chaining and private-field targets remain unsupported, as do inherited/prototype, accessor and proxy traversal or assignment. Forbidden meta-property paths, ambiguous descriptors and malformed or over-budget syntax fail closed; arbitrary Watch text is never interpolated into an assignment.

The official `editor.action.refactor` / **Refactor** workflow is now wired for managed JavaScript and TypeScript documents with the VS Code `Cmd+Shift+R` default on macOS and `Ctrl+Shift+R` elsewhere. Monaco invokes the language-server refactor provider only from editor text focus, and every offered or resolved action retains its exact workspace owner, root, language-server session, active model, path and document version. Resolve may only enrich the same still-current action and requested kind; it cannot execute a command. Required edits are applied before any follow-up command, rejected or partial edits suppress that command, and owner/model/version drift during flush, resolution, edit application or the post-edit version handoff fails closed, including a mid-apply owner change.

Node debug sessions now expose the official `workbench.debug.viewlet.action.toggleBreakpointsActivatedAction` / **Debug: Toggle Activate Breakpoints** action on `Ctrl+F9`. Activation is session-local, defaults to active for every accepted session, publishes only after the exact CDP acknowledgement and preserves the persisted breakpoint definitions and enabled flags rather than rewriting them. Bulk breakpoint mutation, restart, run-to-cursor and other debugger controls share admission so conflicting work cannot overlap; failures leave the previous activation state retryable, while termination, replacement, trust/root/owner drift, A→B→A changes and unmount reject stale publication. The strict Node-only IPC maps to exactly one `Debugger.setBreakpointsActive` request, PHP remains unsupported, and backend authorization retains the registered workspace descriptor and exact workspace/session authority plus trust through the CDP acknowledgement, rejecting unregister, re-registration and same-path replacement.

The bottom-panel terminal now supports up to 16 independently mounted terminal tabs. Each tab owns an isolated PTY session, profile, working directory and lifecycle; switching tabs republishes only the active session, while closing uses deterministic MRU fallback and never closes the final tab. Package/test/Node task consumers bind to the exact active terminal owner and are fulfilled in FIFO order without silently replacing queued work; owner epochs, workspace generations, tab identity and live-session callbacks reject stale same-root A→B→A, closed-tab, replaced-session and unmounted publications. The tablist uses proper tab/tablist relationships, one roving tab stop, `aria-selected`/`aria-controls`, Arrow/Home/End navigation, Delete close, explicit accessible New/Close actions and focus restoration after close.

The official `testing.reRunLastRun` / **Rerun Last Run** command is now available for the owned Jest/Vitest Explorer lifecycle with the VS Code `Cmd+; L` chord on macOS and `Ctrl+; L` elsewhere. The palette command is global rather than editor-focus-scoped and becomes enabled only after an exact current `TestRunOk`; it retains one validated immutable all/file/suite/test scope, including parameterized-prefix semantics, and reruns solely through the existing typed Explorer lifecycle. Workspace/root/trust/gateway or discovery/result invalidation clears the memento, while shared single-flight admission, activation generations and captured-sequence stale settlement fence concurrent work, late completion and same-root A→B→A replacement. The controller receives only fail-closed capability/run verbs; scope, result and gateway internals remain private, and no terminal fallback or Explorer toolbar action was added.

The official `testing.reRunFailTests` / **Rerun Failed Tests** command is now available with the VS Code `Cmd+; E` chord on macOS and `Ctrl+; E` elsewhere. It freezes an all-or-nothing plan from the last exact owned `TestRunOk` and canonical non-truncated discovery, maps only failed/error cases to exact test scopes (including safe parameterized-prefix matching), and rejects the whole request on unsafe, ambiguous, unresolved, truncated or overflowing input. The plan is capped at 256 scopes without lossy suite/file compression. Child scopes run serially with unique bounded run owners under the shared single-flight lifecycle, and their results remain private until every child succeeds and the exact workspace, activation, discovery and result ownership is still current. Error, unavailable, cancellation or stale settlement restores the captured state without publishing partial results, problems or status, while preserving any newer invalidation state.

The companion `testing.cancelRun` / **Cancel Test Run** command uses `Cmd+; Cmd+X` on macOS and `Ctrl+; Ctrl+X` elsewhere. It performs real process cancellation against the exact Rust task `runId`/`workspaceId`, including no-shell process-group stop and reap, rather than merely hiding frontend progress. Cancel intent is synchronized across command, coordinator and Explorer state; owner/trust/gateway/discovery/result invalidation and unmount trigger the same stop-and-cleanup path. Running, cancelling and invalidating states are visible in the Explorer, and activation/run ownership plus late-completion fences cover lifecycle cleanup and same-root A→B→A replacement.

The Test Explorer now supports the exact VS Code `@failed` status term, including its placement beside text or comma separators. It combines with the remaining normalized text query using logical AND; unsupported, negated or inexact `@` terms remain ordinary searchable text rather than silently broadening or rejecting the projection. The bounded fail-closed parser rejects malformed Unicode and oversized input, and the panel reports those invalid states explicitly. Rendering keeps only failed test leaves plus the file/suite/workspace ancestors required to navigate them, without mutating the canonical discovery tree. Starting a visible failed row first transfers keyboard focus to the stable filter input before live status projection can remove that row. Filtering is presentation-only: run scopes, last-run ownership and the immutable all-or-nothing **Rerun Failed** plan remain derived from canonical state and are unchanged by the visible projection.

The exact `@executed` term complements `@failed` with VS Code's executed-test projection. It retains every non-idle local result (`running`, `passed`, `failed` and `skipped`) plus its canonical ancestry and excludes only tests that have not run. Exact `@failed` has upstream precedence when both terms are present, independent of their order. Inexact, negated and differently cased forms remain ordinary text, while normalized text, `@doc` and `@openedFiles` continue to compose through logical AND.

The matching VS Code **Current File** Test Explorer projection is complete through the exact `@doc` term. `@doc` means the one active editor document and remains distinct from the broader `@openedFiles` projection. It composes with normalized text and exact `@failed` using logical AND, and a dirty saved-path document remains eligible because the filter selects document identity rather than disk-content parity. Untitled documents, files outside the selected workspace and the absence of an active document fail closed with an announced unavailable-context state. The document is reduced to an owner-bound `WorkspacePathKey`, so the selected workspace's case-sensitivity and Unicode policy govern identity instead of ad hoc string folding, including model and same-root owner replacement.

The exact, case-sensitive VS Code `@openedFiles` term now projects tests from editor resources in every editor group, including preview tabs, rather than only materialized text documents. The bounded owner-bearing snapshot deduplicates aliases with the workspace's `WorkspacePathKey` policy and never publishes a partial result: overflow, unavailable context and malformed ownership fail closed. It preserves upstream behavior where no editor resources leaves the tree unprojected, while resources that contain no workspace-owned test path produce an empty projection. When both document terms occur, `@openedFiles` has upstream precedence over `@doc`; normalized text and exact `@failed` still compose using logical AND.

Document filtering remains an immutable presentation projection. The visible workspace/file/suite/test rows continue to emit the canonical validated all/file/suite/test scopes, while **Rerun Last Run**, the all-or-nothing **Rerun Failed** plan and result ownership remain derived from the unfiltered Explorer state. Neither `@doc`, `@openedFiles`, text, `@failed` nor `@executed` can change an execution plan.

Owned JavaScript test failures now appear directly in the clean active editor. One immutable decoration per source line aggregates same-line failures, shows a sanitized 128-character end-of-line summary, preserves all available detail in a bounded escaped hover and adds a whole-line/error-overview cue. The projection is admitted only for the exact workspace owner, root, `WorkspacePathKey`, current model and saved content; dirty buffers, foreign or malformed snapshots, out-of-range lines, owner/model A→B→A changes and inactive split groups fail closed. Model changes clear and then repaint the live tab, while each editor owns only its own decoration IDs. Hover output is capped at 20 entries and 16 KiB with a truthful omitted count, and the keyboard-accessible Problems view remains the full-detail fallback.

Legacy JavaScript gutter and cursor runs that are admitted to the terminal now invalidate the previous coverage/problem generation exactly once. Dropped, rejected, full-queue, owner-drifted and PHP runs do not invalidate it, so a repaired test cannot leave a stale inline failure while unrelated or unaccepted work cannot erase current results.

Owned Jest/Vitest runs now retain separate bounded 64 KiB UTF-8 tails for stdout and stderr and expose them through the accessible Test Explorer **Show Output** surface. Both pipes are drained concurrently to prevent child-process deadlock, while overflow, lossy-decoding recap and reader failure publish truthful truncation metadata. Error, unavailable, timeout and active cancellation preserve the available partial output; the legacy bare test-run commands remain wire-compatible and deliberately discard it.

Ordinary Explorer runs now use the same exact `runId`/`workspaceId` task authority as **Rerun Failed** and **Cancel Test Run**. Strict IPC decoding rejects malformed keys, owners, Unicode, byte caps and booleans as one envelope. Output publication is activation-fenced before render, so workspace/result invalidation and A→B→A replacement cannot flash an old snapshot. Failed-test batches aggregate child tails atomically under the same fixed cap, including the active cancelled child. The output view renders plain text only, separates stdout/stderr, announces truncation and empty output, supports owner-safe clipboard copy, restores focus on Close/Escape and never uses HTML injection or a live region for passive logs.

Jest/Vitest **Continuous Run All** is now an explicit accessible Test Explorer mode. It performs one immediate owner-bound run, then coalesces non-ignored workspace file changes behind a 250 ms quiet window and reuses the existing one-shot task, cancel, output, problem and result lifecycle instead of creating a second watcher process. Changes during a run schedule exactly one follow-up; failures remain armed without a hot retry loop.

The Continuous Run coordinator owns an immutable workspace epoch and exact run lease. Workspace, root, trust and A→B→A changes retire the old epoch; timer, scheduler and cancellation failures fail closed; stale settlements cannot publish or cancel a replacement. Manual, failed, coverage and debug execution are symmetrically arbitrated, including same-tick starts and external debug sessions. The persistent Start/Stop toggle preserves focus, exposes `aria-pressed`, keeps Stop available while pending or running and reports watching, queued, running and stopping states through a polite live status.

Project-owned `.vscode/tasks.json` process tasks are now available from the Scripts sidebar and Command Palette. Discovery accepts bounded JSONC and exposes display metadata only; executable commands, arguments and environment values never cross into the frontend. Run admission carries the exact workspace, terminal session, task label and SHA-256 configuration revision, and the backend reopens, reparses and resolves the retained workspace file before spawning.

The supported subset is deliberately shell-free: only `type: "process"` tasks are executable, arguments remain literal, `${workspaceFolder}` is the only substitution, working directories and local executables stay inside the retained workspace, and blocked environment controls fail closed. Tasks use the shared terminal admission registry, direct process spawning, process-group stop, trust/workspace/shutdown cleanup and bounded tagged stdout/stderr output with truthful truncation. Scripts/Tasks tabs implement the ARIA tab keyboard model, while Refresh, Run and exact Stop operations remain owner-bound across late replies and A→B→A replacement.

The standard `.vscode/launch.json` file is now imported read-only when `.codevo/launch.json` is absent. The accepted Node subset is intentionally narrow: version `0.2.0`, `node`/`pwa-node`, workspace-confined script launch with literal arguments/environment or loopback-port attach, plus an optional bounded display-safe `preLaunchTask` label. JSONC size/depth/node limits, duplicate-field and duplicate-name rejection, strict known fields and no dynamic substitutions keep unsupported VS Code runtime behavior fail-closed. An existing `.codevo/launch.json` remains authoritative even when it is invalid or unreadable, so fallback cannot mask project-owned configuration errors.

The launch picker distinguishes imported VS Code entries, displays only safe source and pre-launch metadata and reduces rejected entries to a generic skipped count. Imported metadata cannot enter `.codevo/launch.json` persistence. For debugger starts from either the picker or direct F5 configuration resolution, `preLaunchTask` reuses the single Configured Tasks coordinator rather than a parallel executor: the exact task remains visible with its output and Stop action, and debugging is admitted only after an owned successful exit. Missing labels, non-zero exits, errors, cancellation, configuration/workspace/root/trust drift and stale completion fail closed. Run Without Debugging remains independent and does not execute debugger-only pre-launch tasks.

The read-only importer now recognizes one strict VS Code npm launch form: `runtimeExecutable` must be exactly `npm` or `npm.cmd`, and `runtimeArgs` must be exactly `["run", safeScript]` with the backend-compatible bounded ASCII script name. Workspace-relative `cwd` becomes both the internal npm package identity and execution directory; non-empty launch `args`, protected environment controls, arbitrary executables and ambiguous runtime forms fail closed. Direct F5 selects it only when no document/default target matches and exactly one loaded entry is an imported VS Code npm target. Multiple or mixed configurations still require an explicit picker choice, and public choices and DOM retain only safe name/source/kind/pre-launch metadata.

Execution reuses the existing no-shell npm debug resolver and `preLaunchTask` barrier, with no new IPC. The exact `run` verb and its documented `run-script` alias map to the same private script identity. The backend opens a bounded no-follow manifest below the retained package directory, revalidates the exact script immediately before resolution and returns generic bounded failures without exposing the script body, path or environment. This is deliberately an interpreted safe subset, not raw npm CLI compatibility: non-empty script arguments are unsupported because their meaning would drift when mapped to the resolved child program, and npm `pre<script>`/`post<script>` lifecycle hooks are not executed.

The first `skipFiles` compatibility slice covers only Node internals. For a launch request, an absent field preserves the VS Code Node default and the exact `["<node_internals>/**"]` form selects the same filter; `[]` explicitly disables it. Non-empty attach filters, custom patterns, `node_modules` patterns and combined or malformed arrays fail closed. Accepted input becomes one private exact `nodeInternals` enum rather than a persisted glob: it is absent from picker choices, DOM, `.codevo` serialization and Run Without Debugging, crosses the strict TypeScript IPC only on supported launched Node targets and is retained by the defensive private restart recipe.

CDP installs one owned fixed internal-source blackbox pattern during launch startup and waits for its acknowledgement before allowing the runtime to run. This is intentionally not full VS Code `skipFiles` parity: source-map-derived `node_modules` ranges, attach early-event ordering, old unqualified builtin source URLs and remaining Debug Console/Call Stack presentation polish are deferred.

Configured process tasks now support a bounded sequential `dependsOn` subset. One string uses the default single-dependency order or may declare `dependsOrder: "sequence"`; dependency arrays are accepted only with explicit sequence order. The parser caps each task at 32 dependencies and the whole graph at 512 edges. Duplicate, missing, self, cyclic and non-executable references invalidate the affected task, and that failure propagates transitively to all dependents. The frontend independently rejects malformed graph metadata and shows only a bounded **Runs after** summary.

The backend executes the deterministic dependencies-before-parent plan under one exact run owner, configuration revision, terminal admission, start acknowledgement and aggregate output/truncation budget. It preflights every process plan before the first child starts, then reopens and revalidates the retained workspace, trust, exact configuration revision and current step immediately before each spawn. A persistent stop latch is checked before continuation and transferred atomically with child ownership, closing the otherwise dangerous gap between sequential processes. The existing `preLaunchTask` barrier therefore waits for the complete graph and starts Node debugging only when the whole chain exits successfully.

Dependency chains now publish owner-safe active-step observability through the same monotonic event stream. A chain must be running before the frontend accepts step 1, and later markers must advance contiguously under a fixed total; wrong-owner, stale, repeated, skipped, post-terminal and malformed markers fail closed. Configured Tasks keeps the selected root as the active target while its polite status identifies the current step and its aggregate stdout/stderr projections show escaped step boundaries. Markers are independently bounded and do not consume the existing command-output byte/event budget or expose commands, arguments or environment.

The read-only VS Code importer now also accepts an optional strict private `postDebugTask` label for the supported script, npm and loopback-attach Node configurations. The label must be exact, bounded and display-safe, remains application-private and is never interpreted as a task command or exposed as executable payload. Cleanup reuses the single Configured Tasks coordinator, so the exact task keeps the shared Tasks output and Stop flow rather than acquiring a second executor.

The cleanup owner is bound to the exact accepted debug session and also handles a terminal event that arrives before start resolution completes. The task begins from that session's `terminated` event after normal exit, crash, user Stop or external attach Disconnect. An opaque arm-time lease joins that same exact settlement whether termination arrives before acceptance, during Restart or after the stop request; duplicate and foreign events fail closed without exposing session, workspace or task identity.

Configuration Restart is complete for imported launches with `postDebugTask`. One immutable private prepared recipe drives the exact sequence: Stop for an owned launch or Disconnect for an owned attach, await the post-debug task, rerun the pre-launch task, replay the launch and rearm the post-debug task for the replacement session. New debug starts and a second Restart remain blocked throughout the outer flight. A failed, cancelled or unavailable post-task or pre-task, a rejected replacement start, and any workspace, owner, trust, configuration-version or lifecycle drift prevent replay; stale completion cannot publish or clear a replacement.

The first integrated VS Code compound-debugging workflow is complete without approximating a compound through sequential public starts. The strict read-only importer resolves at most 16 compounds, each containing 2–4 unique existing task-free script or safe npm members, requires `stopAll: true`, rejects attach, member-level `preLaunchTask`/`postDebugTask`, unknown, ambiguous and colliding references, and retains defensively deep-frozen recipes privately. One optional display-safe compound-level `preLaunchTask` runs exactly once through the shared Configured Tasks lifecycle. Imported compounds appear as explicit launch-picker choices with a safe member count and pre-task summary; ordinary F5 ignores them and Run Without Debugging is disabled.

A bounded batch IPC starts the group all-or-nothing. Rust revalidates trust, retained-root and grouped-start authority before every member and again before final commit, accepts only the exact supported Node member recipes, and rolls back partial, stale or invalid batches. The sessions-by-ID registry and ordered root index retain exact siblings while normal single starts preserve replace-all behavior. Root stop, trust revocation, workspace disposal and shutdown remove the complete group; exact ownership leases and bounded retired-authority tombstones close teardown and late-Disconnect races.

The picker, global debug-start gate and workbench orchestration now drive that batch boundary directly. The frontend subscribes before invocation, adopts exactly 2–4 unique session IDs under opaque owner leases, compensates a successfully accepted batch if configuration, owner, root or trust becomes stale, and blocks racing single, attach, test and Restart starts. Stop remains usable while a batch is pending; duplicate cancellation coalesces, late acceptance rolls back once, and an active **Stop All** is issued once through the representative session.

One deterministically selected live child is projected through the existing Debug panel, Call Stack, Variables, Watch, Debug Console and control paths. The latest stopped child becomes foreground without exposing any session identity, arguments or environment; the first terminal child enters group-ending state under the required `stopAll`, and the group retires after all terminals drain. Breakpoint mutations and activation plus exception-pause policy fan out deterministically to every live member. A partial or unknown policy synchronization result marks the group divergent, blocks further controls and fails closed through one representative Stop All while exposing only a generic error.

The supported scope is intentionally narrower than arbitrary VS Code/DAP compounds: attach members, member pre/post tasks, `stopAll: false`, compound Restart, Run Without Debugging and extension-defined launch types are not interpreted.

Name-based function breakpoints are consciously excluded from the VS Code JavaScript parity target. The current VS Code JavaScript debugger advertises `supportsFunctionBreakpoints: false`; CDP's experimental `Debugger.setBreakpointOnFunctionCall` instead requires a concrete runtime object ID and is not a safe or reliable name-resolution primitive. A future beyond-VS-Code action may target an already materialized function-valued Variables row under exact pause ownership, but arbitrary function-name evaluation is not planned.

The strict VS Code `skipFiles` subset now covers direct dependency URLs as well as Node internals. Imported configurations accept only the exact `<node_internals>/**` and `**/node_modules/**` forms and reduce them to a closed typed policy; raw globs and regular expressions never cross IPC. Rust maps the policy to fixed bounded CDP patterns and waits for their acknowledgement before running the debuggee. An absent field keeps the Node-internals default, an empty array disables filtering, and Restart plus compounds retain the immutable policy. This first slice does not claim source-map-derived `node_modules` blackboxed ranges.

Imported Node script and npm launches now support a strict `serverReadyAction` / `openExternally` subset for everyday Express development. The configuration must provide a safe literal-prefix numeric-port capture and an explicit HTTP(S) loopback URI format. Import converts these to a private frozen semantic recipe and discards the raw pattern and format; arbitrary regex, remote URLs, credentials, browser-debug actions, attach and compound members are rejected.

The lifecycle subscribes before debugger start, buffers bounded stdout and stderr independently, adopts only the exact accepted session and opens at most one branded loopback URL through the composition-injected adapter. An opaque owner lease rechecks root, workspace, trust, configuration version and epoch immediately before the side effect. Stop, termination, unmount, replacement and A→B→A changes cancel it, while Restart creates a fresh owner and recipe. Picker and public debugger state never expose the pattern, output or URL. Adversarial tests cover early output, foreign sessions, split chunks, cross-stream synthesis, opener failure and stale microtask races.

Native Node watch debugging is now a public direct launch path for the strict `.vscode/launch.json` form with `runtimeArgs` equal to `["--watch"]` or `["--watch", "--watch-preserve-output"]` and a workspace `.js`, `.mjs` or `.cjs` program. The picker and F5 orchestration preserve `preLaunchTask`, `postDebugTask`, breakpoints, exception pause and the supported `skipFiles` policy and recheck exact workspace/session ownership. Picker preparation rejects a dirty exact target, and every dedicated start rechecks that document immediately after the pre-task. The typed frontend invokes the dedicated `debug_start_native_node_watch` command rather than projecting raw runtime arguments, environment, shell commands or a claimed Node version into IPC.

The trusted live-filesystem backend is enabled on macOS and Linux. It retains the registered workspace and entrypoint identities, discovers an eligible managed Node runtime, constructs only `node --watch [--watch-preserve-output] --inspect-brk=127.0.0.1:0 <script>` and owns the complete process group. Windows parity, npm/nodemon/tsx watch modes and arbitrary VS Code launch/DAP extensions remain outside this public slice.

The generation-owned reconnect controller consumes the continuing inspector feed, binds loopback authority plus UUID, handles endpoint-before-close ordering, transfers a monotonic pause floor and keeps one logical session/event identity. Breakpoints, exception mode, breakpoint activation and the fixed internal/dependency filter replay in acknowledged order before the replacement runs. A process-group owner handles cancel, panic, remote disconnect and exact cleanup, while a redacted structured outcome preserves the terminal reason.

The real production-stack Phase 5 test uses that strict launch builder and the production discovery, CDP, replay, controller and supervisor implementations. Changing an imported dependency creates a fresh child PID and inspector UUID, publishes generation two, verifies the same breakpoint again and hits it; Stop reaps the complete group and finishes the logical registry session once.

Internal B2 now moves each real `NodeCdpAdapter` exactly once from startup replay into a bounded serialized command worker. The exact-generation control proxy stays invisible until `RunIfWaitingForDebugger` acknowledges and the staged event transaction seals; buffered events then drain in FIFO order, including a proven sink callback that re-enters active control without deadlock. Queue overflow, deadline, stale authority, response mismatch, remote close and staged-event overflow fail closed. Event overflow closes the transport and feeds the existing reconnect lifecycle instead of retaining a false-active target.

The confirmed native-watch B3-A checkpoint passes 140/140 tests, including the two real Node proofs. The logical watch adapter now provides typed `StackTrace` and `Scopes` inspection under exact generation and pause-epoch authority; the production proof performs both operations on generation one and proves them again after the real target is replaced by generation two. Idempotent target termination/Drop and the activation rollback guard are compiled and verified parts of this checkpoint.

The CDP transport now depends on a neutral cloneable event-sink port instead of the concrete Node watch gate. The watch layer maps all five typed dispositions, capacity overflow irreversibly latches fail-closed health, and the abstraction adds no per-event allocation beyond the existing payload. Source-boundary coverage prevents the concrete dependency from returning.

The macOS IPv4 **Debug: Attach to Node Process** workflow is now complete end to end. Descriptor-bound terminal authority survives workspace rename/replacement, and unpublished PTY children are terminated and reaped at every ownership-transfer failure boundary. Discovery sends a bounded `/json/list` request only after kernel proof binds the exact held loopback socket to the candidate PID and accepted reverse tuple; it accepts bytes only after a consuming post-response proof of the same opaque kernel connection identity and a fresh full process snapshot. Strict list and start commands add latest-wins publication, monotonic trust-generation fencing and bounded one-shot TTL capabilities without holding trust or session locks across network, filesystem or `libproc` work.

The accessible searchable picker exposes only a local presentation ID, bounded label/detail and port. The lease remains in a private application map, is deleted synchronously before start and is invalidated by refresh, close, root/trust/capability or debug-busy drift; process IDs, argv, source paths and WebSocket URLs never cross IPC or enter React state/DOM. Candidate selection uses the shared global start gate and debug-session acceptance Strategy for Node breakpoint filtering, exception-pause policy, early events, exact session adoption and stale-success cleanup. Its independently kernel-bound WebSocket and Runtime PID are proven before Debugger initialization. The accepted session keeps external-process Disconnect semantics, stores no restart recipe and never falls back to the separate manual **Attach by port…** workflow.

The work deliberately does not enable generic `restart: true`, nodemon, `tsx watch` or npm watch. Those tools have different supervisor and inspector-ownership semantics and will not be approximated through the proven native Node path.

Nette Latte now publishes a diagnostic when a static `{control}` or `{form}` name has one exact presenter/control owner and the complete modeled hierarchy proves that its factory is absent. Dynamic and malformed targets, ambiguous or incomplete owners, unresolved `addComponent` calls and stale cached factory sources are omitted.

Independently audited frontend and Rust CAS foundations now exist for future cross-file semantic edits. The immutable single-use lease binds template lifecycle, workspace session/generation, mandatory per-document host epochs, open-owner session/lifecycle/version or closed-owner monotonic revision plus content hash. The Rust foundation adds epoch tombstones across open/closed/remove/commit transitions, canonical workspace confinement, alias-safe template protection, owner-only full-identity `documentVersions`, duplicate-key rejection and an exact shared 16 MiB UTF-8 wire ceiling. Hostile re-entry, replay, mutable edits, malformed backend decisions and A→B→A fail closed. The Nette component-factory action remains deferred because a concrete retained-workspace/open-document adapter must still connect this foundation directly to the transactional writer.

The separate Nette component-factory planner is also independently clean at 256/256 focused tests. It accepts only an exact workspace plus document identity, parser-clean source proofs and a complete namespace-scoped owner/ancestor hierarchy, supports proven BasePresenter chains and explicit PHP/Nette syntax capabilities, and preserves CRLF, indentation and UTF-16 edit positions. It remains intentionally unreachable from code actions or apply commands.

The shared production transaction writer now retains the workspace and every affected parent descriptor through snapshot, stage, commit, rollback and recovery. Descriptor-relative operations and exact identity revalidation close root/parent/cleanup-name/hardlink replacement races; count, path and byte limits precede expensive work, and mode-only mutations participate in commit and rollback. Because no portable atomic unlink-by-open-handle primitive exists, recovery never deletes, chmods or truncates a terminal quarantine entry. Prior versions remain under persistent 8,192-entry/1-GiB per-parent limits and fail closed when manual recovery inspection is required. A visible inspect and verified-cleanup experience remains P2 before public semantic-edit use.

The public native-watch registry/launch factory and transactional live-breakpoint synchronization are wired through the same accepted debug-session lifecycle as ordinary Node launches. On target replacement, breakpoint definitions, breakpoint activation, exception pause and `justMyCode` replay before execution resumes; Variables, Evaluate, Set Variable and Set Expression remain bound to the current generation and pause authority. Native watch deliberately has no generic application-level Restart recipe because Node owns file-change restart. The final command and real-intent audits are clean.

Two low-severity limitations remain explicit. P2: in a trusted writable workspace, an executable pathname can still be replaced in the narrow interval after descriptor-based validation and before the operating system spawn; portable shebang and package-wrapper support prevents using one universal descriptor-execution primitive. P2: strict npm manifest snapshots use no-follow plus inode identity on Unix, while the non-Unix fallback currently has only regular-file, size and modification-time checks; equivalent Windows reparse-point and file-identity hardening remains a portability follow-up before Windows packaging is ready.

The current frontend checkpoint is green: 1,021 of 1,021 test files and 15,317 of 15,317 tests pass. The Rust library checkpoint passes 1,782/1,782 tests. `npm run check`, `npm run lint` and `npm run build` pass; zero-warning Clippy and formatting gates are green.

Measured against editor-core workflows rather than VS Code's extension ecosystem, practical daily JavaScript/TypeScript/Node/Express coverage is approximately 80–85%, and the common single-process Node debugging workflow is approximately 85–90%. These are intentionally not 100% claims: child/multi-process ownership, universal DAP/extension launch types, remote/container runtimes, Windows native-watch parity and several advanced mutation/inspection cases remain incomplete.

## Next parity implementation slice

Prioritize the remaining practical VS Code JavaScript/TypeScript/Node/Express gaps: exact child/multi-process debug ownership, the highest-value missing Variables/Watches actions and the next audit-ranked daily-workflow gaps. Keep `tsx --watch`, nodemon, npm watch, Windows native watch and generic `restart: true` unimported until their distinct ownership and platform strategies are proven. Resume the deferred Nette quick-fix only after this JS/TS/Node phase and only through a synchronous renderer-owned template/owner Monaco lease.

PHP Clover coverage is complete as an end-to-end PHPUnit/Pest workflow. **Run with Coverage** and **Clear Coverage** live in PHP Test Results, publish bounded workspace and file summaries, and project the strict immutable Clover model into clean active-PHP-editor gutter decorations without coupling it to JavaScript coverage. Exact POSIX/Windows path projection, UTF-8/XML/count limits and malformed or unsupported input fail closed. The typed TS IPC adapter invokes a fixed Rust no-shell Artisan/PHPUnit runner; trust, retained-root FD identity, a shared JUnit/Clover admission permit, process-group timeout and reap, private output outside the workspace, report size/identity checks and cleanup on every result path fence the backend. Clear, PHP edits, saves, watcher changes, explicit test requests, workspace replacement and stale completion invalidate publication, while unavailable backend reasons are accepted only as bounded control-free text.
