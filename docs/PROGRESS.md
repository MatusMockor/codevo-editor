# Progress

## 2026-06-15

Completed:

- Initialized Tauri v2 + React + TypeScript + Vite project.
- Added Monaco editor integration.
- Added Tauri dialog plugin.
- Added Rust workspace file repository and commands:
  - `read_directory`
  - `read_text_file`
  - `write_text_file`
- Added Basic-mode workbench UI:
  - activity bar
  - file sidebar
  - lazy directory tree
  - editor tabs
  - Monaco editor surface
  - status bar
  - command palette
- Added command registry with unit tests.
- Added Rust tests for local workspace file repository.
- Updated dependencies to resolve npm audit findings.
- Verified app bundle build with `npm run tauri build -- --debug --bundles app`.
- Added file create, folder create, active-file rename, active-file delete, and workspace refresh commands.
- Extracted workbench orchestration into `useWorkbenchController`.
- Added prompt adapter and Tauri workspace gateway abstractions.
- Added recent workspace restore through local storage.
- Added CSP configuration instead of disabled CSP.
- Added CodeRabbit review loop and fixed valid findings.
- Added accessibility improvements for command palette, file tree, and tabs.
- Added path helper and dirty-state unit tests.
- Added backend file search command and Quick Open UI command.
- Added backend-visible Smart mode state service and Tauri smart mode gateway.
- Added PHP/Composer workspace detection from `composer.json`.
- Added Problems panel and workbench notice model for future diagnostics/status events.
- Added ripgrep-backed text search command and modal.
- Added PHPactor/Intelephense tool detection from workspace `vendor/bin` and `PATH`.
- Added persistent backend-visible workspace trust state and Trust Workspace command.
- Split frontend workspace gateway into focused file, detection, tool, file-search, and text-search ports.
- Added PHPactor LSP planning command that builds a launch command and JSON-RPC initialize request without starting a process.
- Added frontend language-server gateway and status label for PHPactor LSP readiness.
- Added PHPactor setup guidance modal and command-palette action for non-ready LSP plans.
- Added supervised JSON-RPC process transport that frames LSP messages, starts/stops PHPactor, performs the initialize handshake, and reports runtime status through Tauri events.
- Added frontend language-server runtime gateway, runtime status helpers, command-palette Start/Stop actions, and crash notices.
- Added LSP text document sync foundation for PHP `didOpen`, debounced full-text `didChange`, `didSave`, and `didClose` notifications.
- Added LSP diagnostics bridge from `textDocument/publishDiagnostics` into the Problems panel with per-document replacement.
- Added PHPactor capability registry from initialize results for hover, completion, and definition support.
- Added LSP request-response core with hover, completion, and definition Tauri command adapters.
- Registered PHPactor-backed Monaco hover and completion providers gated by runtime capabilities.
- Added app-native PHPactor go-to-definition command that opens target files and reveals target positions.
- Added app/workspace settings gateway with persisted recent workspace and per-workspace Smart mode.
- Added navigation back/forward stack with command-palette actions and keyboard shortcuts.
- Added SQLite workspace index database foundation with migrations, WAL, busy timeout, and file record commands.
- Added shared ignore matcher with `.gitignore` scopes, default heavy-folder excludes, and workspace file-search integration.
- Added workspace watcher abstraction with normalized native/Watchman event shape and Watchman-to-native fallback strategy.
- Added in-memory index job scheduler foundation with watch, metadata, parse, DB-write, and maintenance queues.
- Added index generation guards and guarded SQLite DB-write commits so stale jobs cannot mutate the index.
- Added initial metadata scan service with `.gitignore` filtering, symlink skipping, SQLite file-record writes, completion/failure events, and a non-blocking Tauri start command.
- Added incremental index updates that route watch events through the scheduler and apply guarded SQLite upsert/remove writes for modify, delete, rename, and ignored-file cases.
- Added index progress UI plumbing with a Tauri gateway, command-palette scan action, status bar counts/phases, and Problems notices for scan errors.
- Added tree-sitter PHP parser foundation with tolerant valid/incomplete PHP fixture coverage.
- Added PHP symbol extraction for namespaced classes, interfaces, traits, enums, methods, functions, and constants.
- Added Composer metadata detection for root PSR-4/classmap roots plus lock/installed package metadata as data.
- Added SQLite symbol storage with transactional per-file symbol replacement, rollback coverage, and cascade cleanup when files are removed.
- Added project symbol search over SQLite symbols with ranked class/type/function/method results and a Tauri/TypeScript gateway contract.
- Added PHP tree panel backed by indexed namespaces, types, functions, methods, and constants.
- Added PHP file outlines in the file tree backed by per-file indexed symbols.
- Added soft, PHP-language, and hard workspace reindex commands with background PHP symbol parsing.
- Added a lazy-loaded xterm.js terminal view in the bottom panel with Problems/Terminal switching.
- Added a trusted-workspace Rust PTY service with terminal session start/input/resize/stop commands and output events.
- Added terminal profile listing and selection for default/platform shells.
- Added Settings UI with persisted mode, trust, PHP backend preference, tool paths, extra ignore patterns, and theme selection.
- Added Index health panel with bounded skipped/error details, run logs, and reindex actions.
- Added per-workspace session restore for open tabs, active tab, sidebar view, and bottom-panel view.
- Added theme polish with semantic active-state foregrounds, accessible terminal palettes, first-render system preference handling, and xterm theme updates without restarting the terminal session.
- Added Mockor Editor product identity with app/window/web title metadata, bundle publisher/category/descriptions, custom desktop icon set, and web favicon.
- Added packaged runtime readiness audit for LSP, terminal, watcher, index, trust/settings, security permissions, and release smoke expectations.
- Verified default debug macOS DMG packaging for `Mockor Editor_0.1.0_aarch64.dmg`.
- Documented macOS Developer ID signing, notarization, stapling, credential, entitlement, and release verification plan.
- Documented first-release sidecar/runtime packaging policy for PHP, PHPactor, Intelephense, Watchman, ripgrep, terminal shells, and SQLite index storage.
- Documented first-release manual update policy and future Tauri updater/static JSON signing path.
- Documented Windows/Linux packaging feasibility, local cross-target blockers, required CI runners, and platform smoke checklists.
- Added Mac-only release CI workflow with unsigned smoke and signed DMG release modes.

## 2026-07-21

Completed:

- Added a workspace JavaScript Test Explorer for Jest and Vitest with deterministic workspace/file/suite/test discovery, filtering, source navigation and all/file/suite/test execution.
- Added persistent per-workspace JavaScript test statuses, conservative parameterized-test result mapping, single-run guards and explicit trust gating without auto-running when the results panel opens.
- Added bounded JavaScript test source discovery and report processing, strict typed run scopes, registered-workspace identity checks, process timeout/termination and stale-result isolation across workspace switches.
- Connected JavaScript test discovery to coalesced workspace-file invalidation, including rescan and directory-subtree events, while deferring refresh safely during an active run.
- Expanded Express Routes from active-file parsing to bounded workspace-wide JavaScript/TypeScript discovery with deterministic route identities and file/source ordering.
- Added exact dirty-document overlays that replace one file's indexed Express routes, per-workspace stale-result isolation, watcher-driven refresh and conservative omission of dynamic paths in the initial workspace-wide foundation.
- Added the controlled workspace Express Routes panel with method/path/receiver/package/file/line filtering, keyboard listbox navigation, refresh and loading/error/empty/truncated states.
- Added focused domain, application, IPC-boundary and UI acceptance coverage for the JavaScript Test Explorer and workspace Express Routes workflows.
- Completed project-scoped Node launch configuration management in Settings with accessible list/select/create/edit/delete/default workflows, strict validation for script, Jest/Vitest and npm targets, and explicit revision-aware saves to `.codevo/launch.json`.
- Added owner-scoped atomic no-overwrite configuration creation, conflict-preserving drafts, workspace-trust mutation guards and stale workspace/read/write isolation.
- Added direct `node <workspace-file>` npm debugging plus conservative workspace-local `tsx`, `ts-node` and `ts-node-esm` wrapper execution and child inspector discovery.
- Kept Node debugging fail-closed: no shell execution or unknown wrappers, workspace-confined targets and working directories, bounded arguments/environment/output, protected environment keys, one `127.0.0.1` inspector endpoint, ambiguity rejection and inspector-token redaction.
- Continued hotspot decomposition by extracting workbench debug orchestration, framework-panel actions, trust-gated Rust quality commands and editor smart-indent logic into focused modules; all tracked baselines moved downward.
- Added conservative advanced Express route intelligence for literal local and cross-file router mounts, selected ESM/CommonJS and Express factory aliases, nested and multiply mounted runtime paths, ambiguity/cycle rejection, bounded expansion and immediate dirty-overlay re-resolution.
- Added the first read-only P1 package-management slice: a searchable keyboard-accessible Packages panel grouped into production/development dependencies, detected npm/pnpm/yarn/bun manager, declared range, installed version and installed/missing state, with exact `package.json` key navigation, dirty-manifest overlays and workspace owner/stale guards. It performs no registry requests or mutations.
- Added trust-gated package install, update, remove and outdated operations with manager-specific previews, explicit confirmation, refresh/status handling and strict typed TypeScript/Rust IPC validation. Execution never uses a shell, validates package names, serializes mutations, runs from the retained workspace root, bounds time/output and terminates the whole process group on timeout. Dirty manifests fail closed; modern Yarn reports the missing safe read-only outdated command explicitly while its supported mutation commands remain available.
- Continued hotspot decomposition with focused document-sync contracts, workbench Git file actions, Monaco change mappings, Latte suggestion policy and Rust Git command modules; the structural-size baselines were lowered to the resulting implementations.
- Audited the workspace-source-discovery IPC boundary end to end: outbound limits must be positive, response file/visit counts cannot exceed their requests, bounded reads enforce the requested UTF-8 byte ceiling, and the TypeScript/Rust contracts have matching regression coverage.
- Added the first complete Symfony workspace slice: exact Composer detection, typed console-command/route/service discovery, searchable keyboard-accessible panel tabs, command-palette access and conservative controller/service symbol navigation.
- Symfony Console inspection is explicitly trust-gated and fail-closed. Rust executes `php bin/console` without a shell from the retained registered workspace root, validates current Symfony JSON shapes, bounds time/output/result fields, terminates process groups and enforces both per-workspace and global concurrency limits.
- Added watcher-driven Symfony invalidation for manifests, `bin/console`, `.env*`, `config/**` and `src/**/*.php`, with deferred refresh while closed, workspace-owner/request-sequence guards and no invalidation rerenders in non-Symfony projects.
- Completed an independent Symfony review and fixed every actionable finding: tagged untrusted responses plus an in-panel Trust action, mixed route-controller and empty service-class tolerance, bounded parallel PHP processes, ARIA grid semantics and direct integration/navigation regression tests.
- Continued hotspot decomposition by extracting Symfony navigation and bottom-panel composition, PHP test navigation and the duplicated Rust file-URI decoder; all affected tracked hotspot limits moved downward.
- Added the first Nette operational slice: exact `nette/application` gating, a searchable keyboard-accessible Services panel, command-palette access, exact NEON-definition navigation and conservative resolved PHP-class navigation.
- Reused the canonical path-preserving NEON source loader instead of adding another crawler. Service projection preserves include/merge precedence, supports bounded alias chains and generated services, applies dirty overlays to the include graph and reports total/truncated state explicitly.
- Added Nette watcher invalidation, workspace/request stale guards, root-confined overlays and tests proving standalone Latte projects do not receive operational Nette UI.
- Kept the tracked `App.tsx` and workbench-controller hotspot budgets below their previous baselines while composing the Nette slice through focused application hooks.
- Added a lazy Presenters tab to the Nette operational panel with a searchable keyboard-accessible treegrid for presenter classes, merged action/render methods, handle signals and existing Latte templates, all navigable to exact source anchors.
- Reused the existing bounded presenter and Latte-template discovery paths, added dirty Presenter/Latte overlays, 200 ms edit coalescing and single-flight latest-refresh semantics, and extended watcher invalidation across Nette config, Presenter and template paths.
- Added a strict bounded-directory TypeScript/Tauri/Rust contract. Native enumeration stops after `maxEntries + 1`; Nette inspection additionally enforces directory, entry, per-file and aggregate byte budgets and fails fast after a limit is reached.
- Completed a second independent Nette audit with no remaining critical/high findings and broad Nette/Latte/NEON, App, panel, command and Rust regression coverage.
- Added a lazy Routes tab to the Nette operational panel with a searchable keyboard-accessible ARIA grid for static Route masks and conservative presenter/action targets, plus exact definition and presenter navigation.
- Reused bounded path-preserving PHP discovery, applied dirty route-source overlays, extended Nette watcher invalidation, and serialized 200 ms debounced refresh bursts with owner/request stale guards and latest-only coalescing.
- Added conservative same-document NEON references and atomic rename for uniquely declared parameters and services, including collision/name validation, versioned Monaco edits and stale-workspace rejection.
- Expanded Nette route discovery from legacy constructors to statically proven modern `RouteList::addRoute()` calls. The bounded parser tracks FQCN/import aliases, namespace/case semantics, receiver reassignment and reachability, constructor/chained module prefixes, flat literal targets and positional/named `oneWay`, while dynamic, malformed and unrelated calls fail closed.
- Added fresh bounded cross-file NEON include-graph references and atomic rename for uniquely declared services and dotted parameters. Outgoing/reverse components, dirty overlays, exact owner/trust/version capture, open-model CAS, closed hashes, atomic finalization and rollback prevent partial or stale workspace edits; scratch documents keep the existing local flow.
- Added Nette NEON semantic diagnostics over the same immutable snapshot and a composable rule pipeline for unresolved symbols, same-document duplicates and provable alias cycles. Owner-scoped Monaco markers are latest-only, bounded and cleared on incomplete, stale, closed, switched or disposed state.
- Resumed JavaScript/TypeScript/Node parity with one-click Jest/Vitest line coverage in the Test Explorer, including workspace/per-file summaries, clear handling and navigation to the first uncovered line.
- Added a strict typed LCOV domain/IPC contract with path, byte, file, line-record, safe-integer and aggregate validation. Coverage state is isolated per workspace and rejects stale results across project, trust and clear transitions while preserving physical in-flight state.
- Added a no-shell Rust coverage runner with fixed arguments, registered-root/trust/identity checks, global concurrency control, private app-data output outside the workspace, no-follow bounded reads, canonical source confinement and deterministic cleanup on success or failure.
- Added active-editor JavaScript/TypeScript coverage decorations with covered/uncovered gutter markers, subtle line highlights, bounded Monaco line mapping and cleanup on dirty documents, workspace switches, report clears and model changes.
- Added dedicated coverage invalidation for supported JS/TS sources, package and lock manifests, Jest/Vitest/Vite configuration, TS/JS config files, renames, rescans and directory-structure changes.
- Added VS Code-like Node pause-on-exceptions policies (`none`, `uncaught`, `all`) for launch and live sessions, with per-workspace preference, strict typed IPC and explicit PHP rejection.
- Added strict single-target Node inspector attach from the command palette and launch configurations. Discovery is IPv4-loopback-only, bounded, workspace-confined and rejects ambiguous or changing targets; attached processes remain externally owned and disconnect termination is emitted exactly once.
- Hardened debugger startup cancellation so trust revocation or workspace disposal prevents every later CDP mutation and session registration; Rust target deserialization rejects unknown fields for every launch variant.
- Replaced root-only npm script loading with bounded monorepo package-script discovery for root and nested `package.json` files, deterministic manifest-qualified command identities and watcher invalidation for manifests, lockfiles, workspace metadata, renames, rescans and directory changes.
- Added typed no-shell package-script execution with backend-revalidated trust, workspace/session ownership, manifest and script identity, descriptor-bound package working directories and exact package-manager arguments. Script bodies and shell command strings never cross the frontend IPC boundary.
- Bound package discovery by manifest/script/visit counts and 256 KiB manifest reads, skip ignored/generated/symlinked trees, and keep root/nested package results isolated across overlapping requests, workspace switches and unmounts.
- Hardened debugger breakpoints before advanced behavior expansion: strict nested IPC, exact launch-target shapes, per-session/per-file and UTF-8 bounds, workspace-file/symlink confinement, Node/PHP capability filtering and globally unique IDs.
- Moved live breakpoint synchronization onto per-session operation permits so slow CDP/DBGp calls never hold the global registry lock; stop removes ownership promptly and stale or superseded responses cannot update persisted verification state.
- Made registered workspace aliases globally deterministic and bounded. Explicit alias reuse transfers one lexical owner atomically, while retargeted aliases without re-registration remain tied to the retained descriptor and cannot redirect trust/dispose cleanup.
- Added VS Code-style conditional breakpoint editing from the editor gutter with plain-click toggle preserved, accessible add/edit/clear/remove UI, conditional glyphs and hover text, split-editor/model ownership guards and bounded conditions.
- Replaced blocking package-script execution with a typed cancellable task lifecycle: prompt start responses, workspace-scoped idempotent Stop, exact running/exited/failed/stopped events, atomic live-task caps and bounded stop-before-start cancellation tombstones.
- Added compensating frontend Stop for uncertain start responses, trust/root switches and unmounts. Task ownership remains keyed by workspace and run ID, stop failures remain retryable, and stale events cannot revive a stopping task.
- Added a package-grouped Scripts sidebar with bounded pagination, one expanded package at a time, stable keyboard/ARIA behavior and a globally visible Stop action for a running task even when refresh or truncation removes its discovered row.
- Removed the legacy blocking package-script command and made terminal startup use an explicit frontend acknowledgement before buffered output or terminal status can publish. Natural shell exit now reaps attached task process groups and releases task ownership/caps.
- Added typed Node breakpoint hit conditions with strict `N`, `>=N` and `%N` forms, safe-integer limits, persistence migration, PHP rejection and CDP-side counters that auto-resume filtered hits without debugger UI flicker.
- Added bounded composable Node logpoints with editor-gutter and Debug-panel editing, strict TypeScript/Rust validation, deterministic interpolation output and a response-driven CDP state machine. User pause wins internal work; evaluate/resume timeouts and failures preserve a truthful visible stopped state.
- Added owner-tagged Node package-task output without modifying raw xterm data. A start acknowledgement gate, monotonic per-run event drainer, bounded UTF-8-safe chunks and exactly-once truncation marker prevent early, reordered or unbounded task output.
- Added conservative TypeScript and ESLint package-task problem matchers with byte-safe line assembly, split ANSI/OSC handling, descriptor-confined paths, truthful caps and authoritative final snapshots that populate navigable Problems entries.
- Bound matcher resolution to retained workspace/package directory descriptors, including replacement, symlink-escape and Unix process-group reuse hardening. Natural completion preserves diagnostics while stop, trust revocation, terminal disposal and workspace changes clear the exact owner only.
- Added persistent Node Watch Expressions with accessible add/edit/remove/enable controls, keyboard and focus management, per-workspace migration-safe storage and automatic reevaluation on pause, selected-frame and definition changes.
- Made Watch evaluation side-effect-safe through an exact nested IPC policy and CDP `throwOnSideEffect`, with session/pause/frame generation ownership and serialized stop/replacement permits. PHP automatic Watch evaluation fails closed as unsupported while explicit REPL evaluation remains available.
- Moved Debug Console process output, pending evaluations, results and errors into one application-owned chronological reducer. Console state now survives bottom-panel switches, rejects stale pause generations, preserves the last terminated-session snapshot and enforces one UTF-8-safe truncation policy instead of component-local caps.
- Added pause-owned debugger inspection state keyed by the exact workspace root, session, pause generation and selected frame. Scopes, variables and evaluation share serialized inspection permits, and stop, resume, frame changes, trust loss or workspace replacement reject queued and late results.
- Replaced eager and synthetic debugger expansion with strict bounded variable pages. CDP bounds `Runtime.getProperties` inspection and DBGP uses real protocol paging; both retain child references only for the returned page and reject stale owners before and after adapter I/O.
- Added a shared accessible Variables and Watch-value tree with owner-bound Load more and retry actions, roving focus, full tree keyboard navigation, truthful loading/circular/depth/limit/stale states and a hard 500-row DOM cap.
- Added direct Jest/Vitest debugging for the selected Test Explorer file, suite or test through an exact typed TypeScript/Rust contract. The launch path accepts no user arguments, environment or working directory, confines execution to the captured monorepo package and registered workspace without a shell, runs Jest serially and Vitest with one inspector worker, and preserves trust, owner and stale-start guards.
- Hardened debugger session startup with per-root physical serialization, latest-start-wins invalidation and a bounded global startup cap. Lifecycle transitions fail closed, pending and live event delivery are byte/count bounded, `Started` precedes buffered output, terminal delivery is exactly once, overflow emits one diagnostic, and sink panics cannot retain or grow queued state.
- Added paused Node debug hover evaluation for bounded JavaScript and TypeScript member expressions. The dedicated Monaco providers coexist with LSP hover, evaluate with the side-effect-safe Watch policy and reject stale workspace, session, pause, frame, model and document-version results. Source masking, latest-wins cancellation, a global four-request cap and bounded untrusted Markdown prevent executable, stale or oversized content from leaking into the editor.
- Added bounded paused Node inline values on the selected frame's stopped JavaScript/TypeScript line. Automatic loading is limited to page zero of the first two distinct non-expensive root scopes (at most 200 variables), never evaluates expressions or follows nested references, and displays at most 12 cached values.
- Inline decorations are owned by the exact workspace, session, pause generation and selected frame and remain trust-gated. They clear on resume, frame/path/tab/model/source invalidation or dirty content, use non-growing Monaco stickiness, and sanitize malformed Unicode, control characters, bidi formatting controls and Unicode line separators before rendering bounded text.
- Added Select and Start for named Node launch configurations in the Debug panel. All validated script, test, npm and loopback-attach configurations are available without exposing arguments or environment values to the DOM; the application retains complete targets privately and reuses the existing no-shell mapper.
- Named and F5 configured launches now fail closed across trust loss, same-root workspace-owner replacement, read-port changes, active sessions and stale requests. Discovery remains bounded to 256 KiB and 64 entries, forged directory-entry paths are ignored, unsafe display names are rejected, start errors cannot disclose launch secrets, and PHP/Nette-only workspaces do not show the Node selector.
- Added owner-scoped Jest/Vitest problem ledgers and navigable Problems projection. Successful all/file/suite/test results replace only their authoritative scope; stale, failed and unavailable runs preserve known state, invalidation advances generation while the explorer is closed, bounded overflow remains truthful, and manual Clear stays dismissed for the same snapshot across A→B→A workspace switches.
- Added toolbar-first Node Debug Restart for the exact active owned session. Restart privately retains an immutable validated launch target, stops the matching session, rechecks the exact root, session ownership and trust state, then replays through the existing launch path with single-flight protection.
- Restart keeps arguments and environment secrets out of UI state and errors, fails closed across stale lifecycle transitions, and performs bounded cleanup for any orphaned replay session.
- Added the command-palette `debug.restart` / **Debug: Restart** path with `Shift+Cmd+F5` on macOS and `Shift+Ctrl+F5` on Windows and Linux. It is enabled only for a trusted workspace with the exact owned running or stopped Node session and retained restart target, stays disabled while start, restart or active-stop work is in flight, and exposes no launch arguments or environment values.
- Added **Debug: Select and Start Configuration** as a searchable keyboard-first command-palette picker over validated named Node launch configurations. The picker exposes only bounded safe metadata while full arguments and environment values remain in the private application-owned target map; only an explicitly accepted selection can start, and exact workspace-owner, trust and read-port identity are revalidated so lifecycle or focus changes fail closed.
- Added VS Code-like bulk breakpoint management to the Breakpoints toolbar and command palette through `debug.enableAllBreakpoints`, `debug.disableAllBreakpoints` and `debug.removeAllBreakpoints`. Actions are count-aware and single-flight, persist the exact workspace breakpoint model, synchronize the exact old/new file union with at most four concurrent live-session requests, and reject stale owner, trust, root, stop and superseded-response work without extending the backend lifecycle.
- Added Node-only `debug.runToCursor` / **Debug: Run to Cursor** with `Ctrl+F10` on every platform. It is available only for the exact trusted, owned, paused Node session and a clean writable workspace-confined JavaScript/TypeScript document at the captured line and column; workspace, session, pause generation, document identity/version/content, cursor and trust are rechecked before mutation.
- Run to Cursor maps both source line and column through the active source map, installs and removes only an ephemeral CDP breakpoint, never changes persistent breakpoints, and continues only after exact resolution. Missing resolution and stale ownership fail closed; if the temporary breakpoint cannot be removed after a bounded retry, the debug session is terminated instead of leaking hidden debugger state.
- Completed independent adversarial reviews of bulk breakpoint management and Run to Cursor, including command/UI enablement, concurrent and stale lifecycle boundaries, strict TypeScript/Rust IPC validation, source confinement, source-map positioning, temporary-breakpoint races and cleanup failure behavior; no unresolved critical/high finding remains.
- Added VS Code-compatible debugger stepping defaults: **Debug: Step Into** uses `F11` and **Debug: Step Out** uses `Shift+F11`, while fresh bookmark defaults moved to `Alt+F11` and `Shift+Alt+F11`. Continue, Step Over, Step Into and Step Out now share the exact trusted, owned, stopped-session and mutation-pending gate.
- Replaced single-winner shortcut dispatch with a deterministic reverse Chain of Responsibility: the newest enabled matching command wins, disabled candidates fall through to older enabled legacy or custom bindings, and known all-disabled shortcuts are consumed safely. Monaco's editor-focused `F12` path uses the same chain, keeping Go to Definition behavior consistent with the workbench dispatcher.
- Added **Debug: Focus Console** with the VS Code platform defaults `Shift+Cmd+Y` on macOS and `Ctrl+Shift+Y` on Windows and Linux. A frontend-only, owner-safe focus generation opens the Debug bottom-panel view and focuses the appropriate console surface exactly once after either an immediate render or a delayed mount: the expression input while paused, otherwise the console output. Workspace replacement invalidates stale requests without adding IPC.
- Added **Debug: Clear Console** in the command palette and an accessible Console-header button over the same owner-safe clear operation. Empty consoles remain a disabled no-op, evaluation history is preserved, late settlement from work pending before Clear cannot repopulate the cleared snapshot, and later session output remains visible.
- Added Node **Run Without Debugging** on the VS Code `Ctrl+F5` default. Config-first target resolution reuses validated `.codevo/launch.json` script, Jest/Vitest and npm entries before conservative plain active-script or active-test fallbacks; attach, PHP, selection-only, dirty and inspector-enabled targets fail closed.
- Runs use a distinct typed Node terminal-task lifecycle rather than creating a debugger session. Three strict commands cover start, start acknowledgement and stop, while one bounded owner-tagged status event reports running and terminal outcomes; existing terminal output delivery is reused and neither inspector endpoints nor launch arguments/environment secrets are reflected into UI or status payloads.
- Node runs and package-script tasks share the same per-workspace/terminal-session admission lease. Exact trust, registered workspace, terminal, run-id and request-generation guards cover resolution, start, acknowledgement, events, stop, workspace replacement and disposal, including compensating cleanup for uncertain or stale starts.
- Added **Run: Stop Without Debugging** to the command palette and an accessible Node lifecycle chip in the Status Bar. Resolving and terminal-wait phases can be cancelled before backend start; starting and running phases stop the exact owner, and sanitized labels expose no target, argument, environment or failure-detail secrets.
- Stop uses an explicit `stopping` state with a retryable failure variant. Duplicate requests are suppressed while a stop is pending, a rejected request re-enables both command and chip with an accessible retry label, and late `running` events cannot revive either normal or retryable stopping state. The UI reuses the existing owner-safe stop command and status event, so no IPC was added.
- Added **Run: Select and Start Without Debugging** as a searchable keyboard-first picker for named Node configurations. Run and Debug share one private-target picker engine through intent-specific Strategy adapters and one workbench-instance coordinator, so opening either picker revokes the other without leaking full launch targets into public command or UI state.
- The Run picker exposes only bounded names and safe target metadata; arguments, environment, working directories and resolved paths remain in application-owned refs. Attach, PHP and inspector-enabled configurations are excluded, while exact workspace identity, trust, retained read port and path containment are revalidated before the existing owner-safe Node-run lifecycle accepts a start. The slice is frontend-only and adds no IPC or Rust surface.
- Added a secondary **Run: Select and Start Without Debugging** action to the Run and Debug toolbar through a narrow Command adapter. It is visible only for JavaScript/TypeScript workspaces, works without an active document, rechecks live `canOpenPicker` state at the click boundary and only opens the existing picker; it never starts a target implicitly.
- The toolbar projection contains only `canOpenPicker` and `openPicker`, so launch choices, paths, arguments, environment and working directories cannot enter toolbar state or the DOM. It reuses the existing instance-scoped Run/Debug picker coordinator and adds no IPC or Rust surface. An independent adversarial audit found no P1/P2 issue, including accessibility and existing Debug-panel regressions.
- Added `debug.configureNodeLaunchConfigurations` / **Run: Configure Node Launch Configurations** to the command palette and an accessible Run and Debug toolbar gear. Both entry points use one controlled, revision-aware configuration dialog, close either open Run or Debug configuration picker first, and never write a configuration or start a target merely by opening it.
- The dialog is scoped to the exact workspace owner and JavaScript/TypeScript capability and fails closed when either drifts. Untrusted workspaces remain read-only, focus is trapped while the dialog is open, Escape closes it, and focus is restored to the opener. The slice adds no IPC or Rust surface; an independent adversarial audit found and fixed two P2 issues.
- Added `debug.addToWatchAtCursor` / **Debug: Add to Watch** over a synchronous scoped Monaco capture of the exact model, URI-bound identity, version, dirty content, cursor, document path and editor/workspace owner. The existing bounded side-effect-safe parser accepts conservative JavaScript/TypeScript member expressions and the conventional end-of-token caret while rejecting executable, masked, incomplete, oversized and stale input.
- Add to Watch requires neither trust nor a paused session because it only persists an enabled definition and never evaluates directly. The reducer-authoritative boolean add is atomic across duplicate, count and payload limits, so the Debug panel opens only after a real transition; automatic evaluation remains exclusively in the existing trusted, stopped Node lifecycle.
- Independent adversarial review covered double-read TOCTOU, split focus, A→B→A and same-root replacement, persistence rollback, stale command closures and source projection. It fixed editor-owner mismatch, synchronous duplicate acceptance and end-caret behavior. The command has no shortcut and adds no IPC or Rust surface.
- Added the official menu-only `debug.addToWatchExpressions` / **Add to Watch** action to Node Variables value rows. It consumes only the backend `evaluateName`, binds exact workspace/session/pause/frame/generation/epoch/row identity and delegates through the existing atomic Watch add lifecycle; scopes, status/load-more rows, PHP, duplicates, full collections and stale focus fail closed. It is hidden from Command Palette, has no shortcut and adds no IPC.
- Added official `testing.runAtCursor` and `testing.runCurrentFile` commands with the VS Code `Cmd+; C` / `Cmd+; F` chords on macOS and platform-mapped `Ctrl` chords elsewhere, restricted to `editorTextFocus`. Exact bounded editor capture, clean saved-file disk parity, owner, trust and activation checks derive typed test/file scopes before a private runner bridge uses the mounted Test Explorer's existing discovery/run/results lifecycle. No terminal fallback, source projection, public runner surface or new IPC was added.
- Independent audit found and fixed trust/workspace-root/workspace-ID A→B→A activation fencing plus missing command-registry shortcut metadata. Canonical scope, disk parity, single-flight, unmount, private bridge, editor focus and platform mapping now have no open P0–P3 finding.
- Completed PHP Clover coverage from PHP Test Results through the active editor. **Run with Coverage** and **Clear Coverage** expose bounded workspace/file summaries and clean-PHP-editor gutter decorations; the strict immutable parser and path projector remain independent of JavaScript coverage and fail closed on malformed XML, unsafe POSIX/Windows paths, invalid UTF-8/counts or any configured budget.
- Added a strict TS IPC adapter and fixed-argument Rust no-shell Artisan/PHPUnit runner. Execution requires workspace trust and retained-root FD identity, shares one fail-closed admission permit with ordinary JUnit runs, kills and reaps the isolated process group on timeout, writes only to a private bounded report outside the workspace, verifies the opened report identity/size and cleans it on every result path. PHP edits, saves, watcher changes, explicit test requests, Clear, workspace replacement and stale completion invalidate publication; unavailable reasons cross the application boundary only as bounded control-free text.
- Added the official VS Code `editor.debug.action.selectionToRepl` / **Debug: Evaluate in Console** flow. It reads the exact live Monaco model, range and scoped editor/workspace owner synchronously, evaluates the selected text, and falls back to the current line only when the selection is geometrically empty; a non-empty whitespace-only selection fails closed instead of silently changing meaning.
- Evaluation is available only for the exact trusted, owned and stopped Node session, pause generation and selected frame. Two identical live captures and two identical debug-owner reads are required before dispatch, while the existing Debug Console `submit` and owner-safe focus operations provide one side-effect-capable single-flight path.
- The existing typed expression contract rejects multiline and control-bearing input consistently, so this frontend-only slice adds no IPC or Rust surface. Adversarial audit fixed retry state after a synchronous focus throw and the whitespace-selection fallback, then hardened top-frame ownership and synchronous/asynchronous submit failures without allowing duplicate evaluation or unhandled rejection.
- Added the paired official VS Code `editor.debug.action.goToNextBreakpoint` / **Debug: Go to Next Breakpoint** and `editor.debug.action.goToPreviousBreakpoint` / **Debug: Go to Previous Breakpoint** commands. Navigation considers enabled breakpoints only, prefers the strict next/previous line in the current file, then walks canonical file paths lexicographically and wraps; breakpoints on the current line are skipped unless one current-line breakpoint is the entire list, where the official self-wrap behavior is preserved.
- A minimal live editor reader captures only the focused JavaScript/TypeScript model identity, version, canonical document/root ownership and current line. Target selection uses a private immutable breakpoint snapshot, and the owner-safe opener may commit only while the selected target still exists at the same canonical path and line and remains enabled. Neither command requires workspace trust or a debug session, has a default shortcut, projects private condition/logpoint data, or adds IPC/Rust surface.
- Navigation is globally single-flight across both directions. Adversarial audit fixed simultaneous Next/Previous dispatch, synchronous and asynchronous navigation-port failures, and canonical workspace-path alias handling while retaining deterministic ordering, owner drift rejection and retry after settlement.
- Added the official `debug.copyStackTrace` / **Debug: Copy Call Stack** command and accessible Call Stack toolbar action over a pinned pure formatter. It copies frames in original order as `name (full fsPath:1-based-line)`, omits columns, emits only `name` for fileless frames, uses deterministic LF separators and rejects empty, malformed or more than 1,000-frame snapshots without mutating them.
- Clipboard access follows dependency inversion: a generic domain port is consumed by the application coordinator, while App explicitly injects the browser clipboard adapter. Copying works for generic owned Node and PHP stopped pauses without requiring trust, but exact root, session, pause generation and workspace owner—including same-root A→B→A replacement—must survive two deep captures before one write. The operation is globally single-flight and releases for retry after synchronous or asynchronous failure.
- The command has no shortcut, and neither command nor toolbar projects frames, arguments, environment, variables, conditions or log messages. Adversarial audit fixed stale inspection-owner acceptance, throwing frame access and the application/infrastructure dependency direction while preserving one-write behavior and the existing manual-debug lifecycle.
- Added official `editor.debug.action.toggleInlineBreakpoint` / **Debug: Inline Breakpoint** on `Shift+F9` with VS Code's add-only behavior. Exact duplicates are no-ops, cursor column 1 creates a line breakpoint, later columns create independently addressable inline siblings, and ordinary `F9` never removes inline siblings.
- Exact location identity now flows through persistence, navigation, rollback, Monaco decorations and ID-based edit relocation. Live capture and relocation are fenced by model/version, editor and workspace ownership, monotonic focus epoch and collision checks; A→B→A, permanently latched epoch exhaustion, stale models and unmount fail closed without relocation loops. Adversarial review fixed the exhausted reader being republished by a later unrelated runner update.
- The existing `debug_set_breakpoints` IPC carries a strict optional non-zero `u32` column without adding a command. Node/CDP preserves raw and source-mapped columns through immediate, early and late resolution; line breakpoints never acquire columns. PHP/DBGp rejects inline locations explicitly, and unrecoverable CDP removal terminates before re-arm to prevent ghost breakpoints. Independent frontend and cross-layer adversarial audits passed without a remaining defect.
- Added the four official Call Stack commands `workbench.action.debug.callStackTop`, `callStackBottom`, `callStackUp` and `callStackDown`, with official labels and no default shortcuts. A pure immutable selector follows adapter order, wraps Up/Down, accepts fileless frames and rejects empty, unknown, duplicate or invalid snapshots.
- The command adapter reuses existing frame selection and fences the exact snapshot, session, pause generation, root, owner, selection and target under one shared flight, adding no IPC or Rust surface. DebugPanel adds one roving tab stop, `aria-current` and Arrow/Home/End focus movement; selection changes and removed frames restore focus to a safe visible fallback. Independent audits fixed rerender/removal focus drift and verified live enablement, mixed-command races and public-state privacy.
- Added official Node-only `workbench.action.debug.restartFrame` / **Restart Frame** as an accessible inline Call Stack action. It has no shortcut, stays out of Command Palette, targets only the current selected/top restartable Node frame, excludes label/subtle frames and exposes no frame data through its public command port.
- The strict request contains exact root, session, pause generation and frame ID. A shared debug-control flight spans CDP ACK through the fresh pause or termination; accepted ACK invalidates all old frame/scope/variable authority. CDP maps the logical ID to `callFrameId`, sends `Debugger.restartFrame` with `mode: StepInto`, and labels only the resulting pause `restart`; bounded marker cleanup prevents unrelated later pauses from being mislabeled. PHP/DBGp is explicitly unsupported.
- Adversarial audits fixed ACK timeout marker leakage, stale-pause invalidation, pending/trust UI propagation, duplicate malformed-frame actions and registry bypass of presentation hints. Extracted lifecycle/CDP modules keep `useDebugSession.ts` and `debug_cdp_transport.rs` below the structural new-file limit without a baseline exception.
- Added official JavaScript/TypeScript **Organize Imports**, **Sort Imports** and **Remove Unused Imports** commands, including `Shift+Alt+O` for `editor.action.organizeImports`. The explicit LSP runner is single-flight and applies only deterministic current-file text edits under exact workspace/session/model/content/sync-version/trust/read-only authority; disabled, stale, command-bearing, cross-file, file-operation and ambiguous path-alias actions fail closed.
- Hardened central workspace identity with explicit POSIX, Windows-drive and UNC flavors plus immutable anchors. Cross-flavor containment, remote-authority confusion and encoded-separator traversal are rejected, while valid POSIX backslash filenames and native Windows/UNC aliases round-trip under the authoritative case/Unicode policy.
- Added `testing.debugAtCursor` / **Debug Test at Cursor** for saved Jest/Vitest tests. The bounded resolver selects the deepest containing or nearest preceding declaration, rejects ambiguous exact/prefix filters and feeds the existing selected-test debug lifecycle without fabricating Explorer nodes. Runner probing and disk parity are bounded, and exact activation/editor/workspace/trust/debug-admission ownership covers concurrent starts and A→B→A replacement. Its official `editorTextFocus` chord is live as `Cmd+; Cmd+C` on macOS and `Ctrl+; Ctrl+C` on Windows/Linux.
- Added one shared real one- or two-stroke keymap model across parsing, defaults, collision detection, Settings capture, Monaco native chord registration and workbench dispatch. Audit fixes add capture-side `stopPropagation()`, map custom `Cmd` chords to `Ctrl` on Windows/Linux, normalize every persisted Settings binding for the resolved platform so mixed `Cmd`/`Ctrl` spellings report the same non-mac conflict, retain exact-plus-prefix conflicts, accept bare `F` as a first stroke and fence observed keymap/editor A→B→A replacements. A two-second timeout and exact captured Monaco text-input owner fence stroke two; wrong strokes, repeats, Escape, blur, editor/keymap replacement and unmount reset pending state without leaking editor-scoped commands into widgets or other inputs.
- Added official npm tree **Open Script** behavior. Click, Enter and Space open the exact script key in root or monorepo `package.json`, while Run/Stop remain explicit execution actions. Bounded JSONC parsing, authoritative open-document overlays, exact-workspace-id read leases, watcher invalidation and StrictMode/latest-wins owner fencing prevent stale navigation. Script discovery works in untrusted JavaScript/TypeScript workspaces, but execution and task/output/problem subscriptions remain trust-gated.
- Added official editor-context `npm.runSelectedScript` / **Run Script** for local writable `package.json` files. The command is hidden from Command Palette, unbound by default but user-bindable, and uses VS Code's `navigation@+1` placement. It resolves the live dirty buffer at the exact selection anchor through the bounded JSONC parser, reconciles one canonical discovered script and reuses the typed package-task lifecycle under strict path, trust, task, authority/discovery-generation and A→B→A fences. Adversarial audits fixed manifest spoofing, mutable authority aliasing, invalid-owner warnings, mixed descriptors, selection-end drift and a synchronous task-admission latch.
- Added the official `workbench.action.debug.disconnect` / **Debug: Disconnect** command for exact accepted Node attach sessions. It shares the VS Code `Shift+F5` default with **Debug: Stop** under mutually exclusive attach/launch command context; disabled and custom collisions are consumed deterministically by the existing dispatcher.
- The Run and Debug toolbar now presents a dedicated accessible Disconnect icon, label, title and busy state only for attach ownership. Its explicit error-handled callback remains retryable after failure, uses the same single-flight control/stop fencing as Stop and stays available for safety cleanup after trust revocation.
- Added strict `debug_disconnect` IPC carrying only `{ rootPath, sessionId }`. Frontend and backend independently require the exact workspace, session and accepted attach mode; same-root workspace replacement, event-before-start-resolution, stale A terminal/completion after B replacement and concurrent duplicate cleanup cannot stop or clear B, and terminal publication remains exactly once.
- Node attach continuation now has real runtime evidence: startup completes `Runtime.runIfWaitingForDebugger` plus `Debugger.resume`, disconnect closes the CDP transport without killing the externally owned process, and the same live Node process can be discovered, reattached and disconnected again. Launched sessions retain Stop semantics and external ownership is never fabricated.
- Hardened Disconnect authority around the workspace registry's retained file descriptor. A renamed or missing root can still dispose its exact session without reopening the path, while alias retargeting/re-registration produces a different authority and cannot disconnect the retained owner; hostile roots fail before lookup and concurrent finish/disconnect remains exactly-once.
- Added the official `workbench.debug.viewlet.action.copyValue` / **Copy Value** command for Node Variables and Watch. It is hidden from Command Palette and unbound by default; focused real value treeitems own local `Ctrl+C`/`Cmd+C`, while edit inputs, controls, selected text, scopes, status and load-more rows preserve native behavior.
- Variables and Watch now share an accessible exact-target **Copy Value** context menu with managed focus, Escape/outside dismissal and owner-safe invocation. Enabled current successful Watch roots and nested variable nodes participate; pending, error, disabled, stale-owner/frame/revision results do not. Focus loss, unmount, A→B→A replacement and menu/blur/owner races revoke stale candidates.
- Added official `debug.copyEvaluatePath` / **Copy as Expression** support for Node Variables, nested Watch values and JavaScript/TypeScript debug hovers. Only adapter-provided `evaluateName` metadata is copied; Watch roots, PHP, pending/error/loading and stale results expose no action.
- Hover copying uses successful-result provenance, bounded opaque one-shot tokens and one allowlisted Monaco command. Forged, replayed, stale model/document/owner/pause/frame, A→B→A and disposed actions fail closed, while arbitrary Markdown remains untrusted.
- Copy Value, tree Copy as Expression and hover Copy as Expression share the same clipboard flight. An App-local per-root bridge exposes exactly four frozen safe commands; raw panel surfaces, candidate setters and the internal hover one-shot input never enter the public controller/workbench graph.
- Added official `debug.setVariable` / **Set Value** parity for writable Node Variables with an ordered context-menu action, Windows/Linux `F2`, macOS `Enter`, double-click and an accessible inline editor. Unchanged values do not call the adapter; Escape/blur cancel, pending work is single-flight, current failures retain draft/focus for retry and stale rows close silently.
- Kept variable Set Value authority row-bound. An App-local ephemeral capability publishes only `canBeginEdit` / `beginEdit`; raw variable references, names, setters and candidate factories never enter the controller graph. Exact focus publications, mutation/value/object identity, duplicate-row reorder, stale menu, shortcut bubbling, owner/pause/frame drift and A→B→A replacement fail closed. Watch roots never acquire `debug.setVariable`, and Hover remains outside mutation.
- Added exact paged/legacy cache reconciliation after a successful assignment. The authoritative row replaces only its source page slot, old/new/cyclic descendants are invalidated, late parent and child replies are fenced and generation keys are separated from physical request tokens so retry remains possible without bypassing the 16-request concurrency bound.
- Added the strict `debug_set_variable` Rust command and typed mutation provenance for lexical scope slots, writable own object data properties and transitive read-only values. Retained workspace authority, trust-revocation ordering, mutation leases, descriptor revalidation, fixed non-interpolated CDP calls, exact special-value arguments, result-name binding, per-pause quotas and final pause/frame ownership are enforced independently of the UI.
- RHS evaluation is exact-frame and exactly once. Every unprovable result after dispatch is reported as indeterminate without retry, temporary object groups are released on all paths, scope objects are reacquired as fresh live handles before release, and returned references are pause-owned and bounded. A real Node inspector test covers a shadowed local plus primitive, object and nested property results; DBGp reports the capability as unsupported.
- Independent frontend and backend audits fixed REPL/mutation races, raw API leakage, hanging page retry and physical-flight bounds, trust-revocation ordering, stale object-group handles, stale scope snapshots, post-dispatch indeterminate handling, final-owner success races, per-pause quota reset, duplicate shortcut execution, stale capability cleanup, pending-input blur loss, stale context-menu retargeting and nested Watch treeitem event bubbling.
- Projected the exact same `debug.setVariable` capability into writable nested Watch values without adding a Watch-specific raw setter. Disabled, pending, running, PHP, untrusted, revision/frame/workspace/owner drift and removed or reordered definitions remove both the provider and command focus; root value assignment remains a separate `debug.setWatchExpression` contract.
- Added Watch-only backend object-root provenance while keeping the root wire result, REPL, Clipboard, proxy, internal and non-object results read-only. Bounded parent/property lineage enables exact subtree invalidation, preserves unrelated Variables/Watch roots/aliases and prevents stale detached targets from remaining mutable.
- Hardened lineage against provenance laundering and truthful-capability drift. Ancestor cycles reuse references only under identical mutation provenance; private, symbol, accessor, synthetic and read-only back-edges cannot recover a mutable ancestor. One shared 1,024-byte UTF-8 name policy keeps unsupported JavaScript property names inspectable but read-only across both wire and CDP.
- Added mock and real Node evidence for nested Watch property assignment, subsequent Watch paging and unrelated scope mutation. Modular variable-page, Watch mutation test and command extraction keeps the hotspot gate green without increasing its baseline.
- Extended Node variable wire data with bounded backend `evaluateName` accessors for safe bindings, member/numeric/JSON-escaped properties and narrow `this.#private` cases. Synthetic/internal or unsafe paths fail closed, and the pause-scoped object-reference cache deduplicates and caps unique references.
- Added the shared clipboard evaluation context and one global owner-fenced coordinator across Variables and Watch, reusing App's single clipboard adapter with Call Stack copying. Repeated immutable captures bind root, workspace owner, session, pause, frame, generation, epoch, identity and displayed value before one write; failures fall back to the already displayed value and release for retry. PHP/DBGp remains explicitly unsupported.
- Added a bounded host-side CDP preview serializer with no injected page JavaScript, so restrictive CSP remains compatible and getters are not invoked. Depth/node/key/output caps, request/total timeout, proxy/overflow rejection, displayed-value fallback and deterministic `codevo.clipboard` object-group cleanup cover success, exception, unsupported preview and timeout paths.
- Completed Watch-root Set Value Phase 2 through the official menu-only `debug.setWatchExpression` / **Set Value** action. It has no shortcut and is absent from Command Palette; `debug.renameWatchExpression`, F2/Enter and root double-click remain the separate expression-text editor. Nested Watch rows continue to use `debug.setVariable`, and PHP/DBGp reports root assignment as unsupported.
- Extended safe Node Watch-root assignment from captured mutable identifiers to statically proven member, canonical numeric-index and JSON-string-index targets, including bounded nested chains and authoritative selected-frame `this` roots such as `this.prop`, `this[0]`, `this["a-b"]` and `this.nested.leaf`. Bare `this` stays read-only. A strict typed parser caps expressions at 4 KiB, names at 1,024 UTF-8 bytes and traversal at eight segments, accepts only canonical JavaScript-safe numeric indices, and rejects malformed, dynamic or forbidden meta-property syntax. Exact scope/receiver ownership and ordinary own data-property descriptors are proven without executing getters or proxy traps.
- Kept assignment exact-once and owner-safe. Opaque single-use tokens bind the exact definition revision, expression, workspace, session, pause generation and frame; static paths rewalk and identity-check the proven object lineage, pin the final parent before one RHS evaluation and reuse the existing fixed, non-interpolated property mutation path. Every consumed-token attempt revokes related ephemeral authority, while success globally reevaluates enabled Watches and invalidates the old Watch/variable-page cache.
- Kept the residual parity gap explicit: dynamic computed keys, calls, optional chaining, private fields and inherited/prototype, accessor or proxy paths remain unsupported, while VS Code's universal DAP `setExpression` behavior is broader. PHP/DBGp remains explicitly unsupported.
- Added official `editor.action.refactor` / **Refactor** for managed JavaScript/TypeScript documents with `Cmd+Shift+R` on macOS and `Ctrl+Shift+R` elsewhere. Editor-text-focus dispatch and the language-server provider retain the exact workspace owner, root/session, active model, path and version through collection, resolve, edit application and command execution; resolve-only work cannot execute, required edit rejection suppresses its follow-up command, and mid-apply owner/model/version drift fails closed.
- Added Node-only `workbench.debug.viewlet.action.toggleBreakpointsActivatedAction` / **Debug: Toggle Activate Breakpoints** on `Ctrl+F9`. Session-local activation defaults to active and publishes only after the exact `Debugger.setBreakpointsActive` acknowledgement without changing persisted breakpoint definitions or enabled flags; shared control/bulk/restart/run-to-cursor admission, failure retry, termination and A→B→A ownership fences prevent stale state. The backend retains the registered workspace identity, exact workspace/session authority and trust through the ACK, rejecting unregister, re-registration and same-path replacement; PHP remains explicitly unsupported.
- Added bounded Terminal Tabs with at most 16 isolated PTY sessions, independent profile/CWD/runtime state, active-session-only routing and deterministic MRU close fallback. Queued task consumers are owner-bound and fulfilled FIFO, while workspace owner epochs, generations, live tab/session identity, close, replacement and unmount fence stale callbacks. Accessible tablist semantics include a roving tab stop, selection/control relationships, Arrow/Home/End navigation, Delete close, explicit New/Close labels and focus restoration.
- Added official `testing.reRunLastRun` / **Rerun Last Run** for the owned Jest/Vitest Explorer lifecycle with `Cmd+; L` on macOS and `Ctrl+; L` elsewhere. One immutable validated all/file/suite/test scope is retained only after an exact current result and rerun through the typed Explorer lifecycle; parameterized-prefix semantics survive cloning. Workspace/root/trust/gateway and discovery/result invalidation, shared single-flight admission, activation generations and captured-sequence stale settlement fence late results and same-root A→B→A replacement. The workbench sees only fail-closed capability/run verbs, with no terminal fallback or toolbar action.
- Added official `testing.reRunFailTests` / **Rerun Failed Tests** with `Cmd+; E` on macOS and `Ctrl+; E` elsewhere. It derives one immutable all-or-nothing plan from the exact owned `TestRunOk` plus canonical non-truncated discovery, maps only failed/error cases to exact test scopes with safe parameterized-prefix support, and rejects unsafe, ambiguous, unresolved, truncated or overflowing input. Plans are capped at 256 scopes without suite/file compression and run serially with unique bounded child owners under shared single-flight admission. Results are staged privately and published atomically only after every child succeeds with current workspace, activation, discovery and result ownership; error, unavailable, cancellation and stale settlement restore captured state without partial result/problem/status leakage while preserving newer invalidation.
- Added official `testing.cancelRun` / **Cancel Test Run** with `Cmd+; Cmd+X` on macOS and `Ctrl+; Ctrl+X` elsewhere. It stops and reaps the real no-shell Rust task process group through the exact `runId`/`workspaceId`; synchronized cancel intent, running/cancelling/invalidating Explorer states, owner/trust/gateway/discovery/result invalidation, unmount cleanup and late-completion fences cover cancellation and same-root A→B→A lifecycle replacement.
- Added the exact VS Code `@failed` Test Explorer filter term, including adjacent text and comma placements. It intersects with remaining normalized text using logical AND, while unknown, negated, differently cased or inexact `@` terms remain ordinary text. The bounded fail-closed parser reports invalid input explicitly, and the immutable render projection retains only failed leaves plus their file/suite/workspace ancestors. Running a visible failed row moves focus to the stable filter before live projection removes it; the canonical discovery tree, run scopes, last-run memento and all-or-nothing **Rerun Failed** ownership remain unchanged.
- Added the exact VS Code `@doc` **Current File** Test Explorer filter as a distinct term from `@openedFiles`. It intersects with normalized text and exact `@failed` using logical AND. The one active workspace-owned saved-path document is eligible even while dirty because selection follows document identity, while untitled, outside-workspace and absent active documents fail closed with an announced unavailable-context state. Identity is owner-bound and follows the selected workspace's `WorkspacePathKey` case-sensitivity and Unicode policy across document, workspace and same-root owner replacement.
- Kept `@doc` strictly presentation-only. It projects the immutable visible workspace/file/suite/test ancestry while every row retains its canonical validated all/file/suite/test scope; last-run ownership, the all-or-nothing **Rerun Failed** plan and execution results remain derived from unfiltered Explorer state and cannot be changed by `@doc`, text or `@failed`.
- Added the exact, case-sensitive VS Code `@openedFiles` Test Explorer projection from all editor-group resources, including previews. Its owner-bearing bounded snapshot deduplicates workspace aliases with `WorkspacePathKey`, distinguishes no editor resources from resources with no owned test files, rejects unavailable, malformed and overflowed snapshots without publishing partial results, and gives `@openedFiles` upstream precedence when combined with `@doc`.
- Kept `@openedFiles` presentation-only and independent from canonical all/file/suite/test scopes, last-run ownership and the all-or-nothing **Rerun Failed** plan. It composes with normalized text and exact `@failed` using logical AND.
- Added the exact VS Code `@executed` Test Explorer projection for every non-idle local status (`running`, `passed`, `failed` and `skipped`). It retains canonical ancestry, composes with text and document filters, preserves inexact forms as ordinary text and gives exact `@failed` upstream precedence independent of term order.
- Kept `@executed` presentation-only: canonical row scopes, last-run ownership, the all-or-nothing **Rerun Failed** plan and result publication remain derived from the unfiltered Explorer state.
- Added owner-bound inline JavaScript test failure feedback for clean active editor models. Deterministic same-line aggregation produces one whole-line/overview decoration, a sanitized 128-character end-of-line summary and a full escaped hover bounded to 20 entries/16 KiB with truthful omission text. Dirty buffers, malformed or foreign snapshots, invalid lines, inactive split groups and workspace/model A→B→A transitions fail closed, while model changes clear and repaint only the exact editor's decoration IDs.
- Kept the existing keyboard-accessible Problems view as the authoritative full-detail surface and left canonical test status/run/rerun state unchanged. Terminal-admitted JavaScript gutter/cursor runs now invalidate the old coverage/problem generation exactly once; dropped, rejected, full-queue, owner-drifted and PHP runs do not.
- Extracted runtime-owned editor decoration composition out of `EditorSurface`, and lowered the locked structural hotspot baselines for `EditorSurface.tsx` and `useWorkbenchController.ts` without increasing any baseline.
- Added owner-bound Jest/Vitest **Show Output** for ordinary runs, reruns and cancelled failed-test batches. Rust drains stdout/stderr concurrently, retains an independent 64 KiB UTF-8 tail per stream and publishes truthful truncation plus partial terminal output without changing legacy bare run commands.
- Migrated ordinary Explorer execution onto the exact task owner lifecycle, added strict whole-envelope IPC validation and synchronously activation-fenced output publication across result invalidation and A→B→A replacement. Failed-run output aggregation remains atomic and bounded.
- Added a plain-text accessible output view with separate stdout/stderr sections, empty/truncated states, Copy, Close/Escape focus restoration and an ordinary-run Cancel action. No HTML injection, passive log announcements or public raw task authority were introduced.
- Added bounded Node Debug Console IntelliSense for lexical identifiers and statically safe member paths. Automatic dot/two-character triggering and explicit `Ctrl+Space` use an accessible combobox/listbox without conflicting with REPL history, evaluation, Escape or caret movement; accepting a proposal only applies the exact UTF-16 replacement and never submits it.
- Added a structured exact-key `debug_completions` boundary with no raw expression. Rust enumerates lexical scopes and ordinary own data descriptors without getter, proxy, call or arbitrary-evaluation execution; retained-root/trust/session/pause/frame fences, quotas, full-pipeline deadlines and truthful item/byte incompleteness fail closed, including truncated nearer-scope shadowing.
- Added latest-wins debounce/input/owner coordination across workspace, root, Node adapter, session, pause and selected frame. A→B→A replacement, cursor movement, unmount and stale responses cannot reopen or apply a stale proposal; generic unavailability never leaks backend errors.
- Added explicit Jest/Vitest **Continuous Run All** to Test Explorer. One immediate run and debounced non-ignored workspace changes reuse the existing owner-bound one-shot task, cancel, output, problem and result lifecycle; changes during a run coalesce into one follow-up without a persistent runner watch process.
- Added an immutable epoch/lease coordinator with exact timer and cancellation ownership, fail-closed scheduler/cancel handling and workspace/root/trust/A→B→A retirement. Manual, failed, coverage and debug lifecycles are symmetrically arbitrated, including same-tick starts and external debug sessions.
- Added a persistent focus-stable Start/Stop toggle with `aria-pressed` and polite watching/queued/running/stopping status. Watcher-only invalidation is separate from terminal result invalidation and accepts every non-ignored file because no runner dependency graph exists yet.
- Added bounded project-owned `.vscode/tasks.json` discovery and direct execution for the exact shell-free `type: "process"` subset. The frontend receives display metadata plus an SHA-256 revision only; command, arguments and environment remain backend-private and are reopened, reparsed and re-resolved from the retained trusted workspace immediately before spawn.
- Added strict literal argument handling, `${workspaceFolder}`-only substitution, retained-root working-directory/executable policy, blocked environment controls, shared terminal admission, process-group cancellation and trust/workspace/shutdown cleanup. Tagged stdout/stderr uses 8 KiB chunks under fixed event/byte caps with one truthful truncation marker.
- Added an accessible Configured Tasks surface beside package scripts and dynamic Command Palette Refresh/Run commands. Exact workspace/session/label/revision ownership fences admission, acknowledgement, output, stop and late completion; ARIA tabs support Arrow, Home and End navigation.
- Added read-only import of the standard `.vscode/launch.json` format for a strict bounded Node subset: version `0.2.0`, `node`/`pwa-node`, script launch or loopback-port attach, literal arguments/environment, workspace-confined paths and an optional display-safe `preLaunchTask` label. Unsupported fields, dynamic substitutions, duplicate names, malformed or over-budget JSONC and ambiguous configurations are skipped or rejected explicitly instead of being interpreted speculatively.
- Kept `.codevo/launch.json` authoritative. The VS Code file is consulted only when the project-owned file is absent; an invalid, unreadable, oversized or stale `.codevo/launch.json` never falls through to a different configuration source. Imported source metadata stays read-only and cannot be persisted into the `.codevo` model.
- Made the Node launch picker source-aware: imported choices are labelled as VS Code entries, show their safe `preLaunchTask` label and summarize skipped unsupported entries without exposing launch or task payloads.
- Connected imported `preLaunchTask` to the existing Configured Tasks lifecycle for both picker launches and direct F5 resolution. The exact process task is visible through the shared Tasks UI, output and Stop action; Node debugging starts only after an owned exit code `0`. Missing, failed, cancelled, changed or stale tasks fail closed, and workspace/root/configuration replacement or unmount cancels the barrier. Run Without Debugging deliberately does not acquire this debugger-only pre-launch behavior.
- Added a strict read-only VS Code npm launch import. It accepts only `runtimeExecutable: "npm"` or `"npm.cmd"` with exact `runtimeArgs: ["run", safeScript]`, maps workspace-relative `cwd` to the same internal package identity and execution directory, and rejects non-empty launch `args`, protected environment controls, arbitrary executables and ambiguous runtime forms. Direct F5 uses this fallback only when exactly one imported VS Code npm configuration remains and no document/default configuration matches; multiple or mixed entries are never guessed.
- Routed imported npm launches through the existing safe no-shell npm debug resolver and existing `preLaunchTask` barrier without adding IPC. Both exact npm verbs, `run` and its documented `run-script` alias, map to the same private script identity. The backend reopens a bounded no-follow `package.json` beneath the retained package directory, revalidates the exact script and reports only generic bounded errors. This remains an interpreted safe subset rather than raw npm CLI emulation: extra script arguments and npm `pre<script>`/`post<script>` lifecycle hooks remain intentionally unsupported.
- Added the first bounded VS Code `skipFiles` compatibility slice for launched Node configurations. An absent field uses the VS Code Node default, and the exact `["<node_internals>/**"]` form selects the same fixed Node-internal step filter; an explicit empty array disables it. Custom patterns, `node_modules` patterns, mixed or malformed arrays and every non-empty attach form fail closed instead of being approximated.
- Kept this as a private exact `nodeInternals` policy rather than persisting raw glob input. It is added only to validated imported debug launches, omitted from choices, DOM and Run Without Debugging, decoded exactly at the TypeScript IPC boundary and retained through the private defensive restart clone. CDP installs one fixed internal-source blackbox pattern and waits for its acknowledgement before allowing the launched runtime to run.
- This is not full VS Code `skipFiles` parity. Source-map-derived `node_modules` ranges, attach startup early-event ordering, old unqualified builtin source URLs and further Debug Console/Call Stack presentation polish remain explicitly deferred.
- Added strict sequential `.vscode/tasks.json` dependencies. A single `dependsOn` string uses the default single-step order or explicit `dependsOrder: "sequence"`; arrays require explicit sequence order. Each task is capped at 32 dependencies and the configuration at 512 dependency edges. Missing, duplicate, self-referential, cyclic or non-executable dependencies fail closed, and invalidity propagates to every dependent task instead of producing a partial graph.
- Kept dependency execution backend-owned under one exact task owner, configuration revision, terminal admission, acknowledgement gate and aggregate output budget. The complete chain is preflight-resolved before execution, then the current trusted workspace, configuration and process plan are reopened and revalidated before every individual step. One stop latch survives the gap between child processes, so a racing Stop cannot allow the next dependency step to escape.
- Added a bounded **Runs after** summary to Configured Tasks without exposing commands, arguments or environment. `preLaunchTask` now waits for the complete resolved dependency chain and admits debugging only after every step and the selected task exit successfully.
- Added owner-safe active-step observability for dependency chains. The backend publishes bounded step markers under the same exact run owner and monotonic event sequence; the frontend accepts only a running chain that starts at step 1 and advances contiguously with a fixed total. Configured Tasks announces the current step through its polite live status and inserts escaped step boundaries into the aggregate output without exposing commands, arguments or environment or consuming the existing output byte/event budget.
- Added strict private VS Code `postDebugTask` metadata for imported script, npm and loopback-attach Node configurations. The exact bounded display-safe label remains application-private and is never treated as a task command or executable payload; cleanup reuses the shared Configured Tasks output and Stop flow.
- Bound cleanup to the exact accepted debug session, including a terminal event that races ahead of start resolution. Its `terminated` event starts the task after normal exit, crash, user Stop or external attach Disconnect, while new debug starts and Restart remain blocked until cleanup reaches a terminal state. Stale events and replacement sessions fail closed.
- Completed configuration Restart for imported launches with `postDebugTask`. An opaque arm-time lease joins the exact cleanup settlement across early-terminal and stop-response races, while an immutable private recipe performs Stop for launches or Disconnect for attach, awaits post-task success, reruns the pre-launch task, replays the target and rearms the post-task for the replacement session.
- Kept the complete Restart flight single-owner and fail-closed. Post-task, pre-task or replacement-start failure and workspace, owner, trust, configuration-version or lifecycle drift prevent replay; duplicate, foreign and stale completion cannot start or clear a replacement.
- Completed the first integrated VS Code compound-debugging workflow. The strict read-only importer accepts at most 16 compounds, each resolving 2–4 unique existing task-free script or safe npm members, requires `stopAll: true`, rejects attach, member `preLaunchTask`/`postDebugTask`, ambiguous or colliding references and unsupported fields, and retains defensively deep-frozen member recipes privately. One optional display-safe compound-level `preLaunchTask` runs exactly once through the shared Configured Tasks lifecycle. Ordinary F5 deliberately continues to ignore compounds; they are selected explicitly in the launch picker, and Run Without Debugging stays disabled for them.
- Added a bounded all-or-nothing compound batch boundary and integrated it through the real picker and debug orchestration. Rust revalidates the retained root, trust and grouped-start authority before every member and before final commit, returns only 2–4 exact unique session IDs, and rolls back partial, stale or invalid groups. The frontend subscribes before starting, adopts the exact batch under opaque owner leases, compensates accepted starts that become stale, and prevents single, attach, test or Restart starts from bypassing the same global debug-start gate.
- Projected the deterministically selected live child into the existing Debug panel, Call Stack, Variables, Watch, console and debugger control paths without publishing compound IDs, arguments or environment. Paused-child changes reselect and reproject the exact latest stopped snapshot; the first terminal member enters fail-closed group ending under `stopAll`, and all terminal members retire the group. The panel shows only the safe member count and compound pre-task summary.
- Added real pending and active **Stop All** behavior. Stop remains available from the toolbar, command and shortcut while native batch start is pending, duplicate requests coalesce, late accepted sessions are rolled back once, and an active group is stopped once through its representative session. Breakpoint add/update/remove/toggle/bulk synchronization, breakpoint activation and exception-pause policy fan out deterministically to every live member. A partial or unknown synchronization result marks the group divergent, blocks further controls and fails closed through one representative Stop All; only the generic synchronization error is exposed.
- Refactored the Rust debug registry to maintain exact sessions by ID plus an ordered per-root index. Existing starts preserve replace-all behavior, while grouped-start authority retains siblings. Exact finish/stop preserves other members until the `stopAll` group lifecycle retires them; root stop, trust revocation, workspace disposal and global stop remove all. Root/session checks use exact ownership, ID-only controls serialize with teardown, and bounded retired-authority tombstones keep late Disconnect idempotent without weakening replacement semantics.
- Added the strict VS Code `skipFiles` subset for direct dependency URLs. Imported configurations accept only the exact `<node_internals>/**` and `**/node_modules/**` forms, map them to a closed typed policy and never send raw globs or regular expressions over IPC. Rust installs only fixed bounded CDP blackbox patterns and requires their acknowledgement before the debuggee runs; empty arrays still disable filtering and an absent field keeps the Node-internals default. Restart and compounds preserve the immutable policy. Source-map-derived dependency ranges remain explicitly outside this first direct-URL slice.
- Added a strict `serverReadyAction` / `openExternally` slice for imported Node script and npm launches. The importer accepts only an explicit literal-prefix plus numeric-port capture and an explicit HTTP(S) loopback URI format, converts both to a private deep-frozen semantic recipe and discards the raw pattern and format. Arbitrary regular expressions, remote hosts, credentials, browser-debug actions, attach and compound members fail closed.
- The server-ready lifecycle subscribes before start, retains bounded per-stream early output for at most 16 candidate sessions, adopts only the exact accepted session and authorizes one branded loopback URL immediately before the injected opener side effect. Stop, termination, unmount, Restart, trust, root, workspace, configuration-version and A→B→A changes invalidate the opaque lease. Restart rearms a fresh owner; picker and public debugger state expose no pattern, URL or output.
- Independent adversarial audits fixed a stale microtask opener race, a direct application-level Tauri opener bypass, synthetic cross-stream matches, unsafe encoded path normalization, ambiguous digit-leading suffixes and raw unknown-field diagnostics. Dedicated npm/F5, picker-privacy, unavailable-opener and App→controller→workbench composition tests close the production boundary.
- Completed a non-production Phase 0 proof for native Node watch debugging before exposing any watch configuration. A real bounded Node integration test launches `node --watch` with a loopback inspector, proves distinct supervisor and inspected-child PIDs in one process group, mutates an imported dependency, observes the old WebSocket and child close, discovers a fresh inspector UUID and child, then terminates and reaps the complete process group. The proof passes repeatedly without fixed startup sleeps and fails closed in CI when the required runtime capability is unavailable.
- The proof also confirmed why `tsx --watch` or nodemon could not reuse the then-current one-socket adapter honestly: the production backend stopped consuming inspector endpoints after initial ambiguity validation, and a persistent supervisor could leave an apparently active session backed by a dead CDP socket. At that checkpoint no watch/restart parser or public behavior was enabled; the later generation-owned reconnect controller and public native-only slice supersede this historical restriction.
- Completed the internal native Node watch reconnect runtime without exposing launch syntax. A bounded generation coordinator owns exact loopback authority plus inspector UUID, transfers a monotonic pause-generation floor, buffers the endpoint-before-close race, rejects ambiguity and stale events, and keeps one logical event identity across replaceable CDP transports. Desired breakpoints, exception policy, breakpoint activation and the fixed `justMyCode` filter replay in deterministic acknowledged order before the replacement target runs.
- Added a process-group-owning watch supervisor with a bounded endpoint/disconnect/cancel loop, shared cancellation through connect and replay, exact remote-versus-intentional disconnect handling, paused-generation `Resumed` invalidation, panic-safe cleanup, self-join protection and a redacted structured terminal outcome. Stop and Drop revoke authority before transport shutdown, then terminate, hard-kill if necessary and reap the complete supervisor group.
- Added a strict internal production launch plan for managed Node 22/24 and best-effort 26. It accepts only canonical workspace `.js`, `.mjs` or `.cjs` files and constructs exactly `node --watch [--watch-preserve-output] --inspect-brk=127.0.0.1:0 <script>`; raw arguments, environment, npm, nodemon and tsx remain unavailable.
- Completed the production-stack Phase 5 proof. The test uses the strict launch builder, production inspector discovery, real CDP replay, reconnect controller and supervisor owner. An imported dependency change creates a fresh target PID, publishes generation two, verifies the same breakpoint a second time and hits it, while Stop reaps both supervisor and target and finishes the logical registry session exactly once. The proof also found and fixed an idle-tick bug that incorrectly treated a healthy active generation as a replacement timeout.
- Completed the internal B2 ownership transfer without registry or IPC exposure. Each real `NodeCdpAdapter` moves exactly once from startup replay into one bounded serialized command worker; the exact-generation proxy publishes it only after `RunIfWaitingForDebugger` acknowledges and the staged event transaction seals. Buffered startup events then flush through one FIFO drainer, and a sink callback can re-enter the newly active control surface without deadlock. Queue overflow, deadline, stale authority, response mismatch, remote close and event-staging overflow remain typed and fail closed; an overflow closes the transport and drives the existing reconnect owner rather than leaving an apparently live target.
- Completed B3-A behind the internal native-watch boundary. The logical watch adapter now exposes typed `StackTrace` and `Scopes` inspection through the exact active generation and pause epoch. Real production-stack proof reads both operations from generation one, replaces the inspected Node target, and proves them again against generation two without accepting stale generation-one authority.
- Completed source-level lifecycle hardening: `NodeCdpWatchTarget` has idempotent termination/Drop semantics, and an activation rollback guard retains the worker, control lease and event generation until ownership commits into the target. Early failure revokes installed control, marks the close intentional, closes the generation and performs bounded worker teardown.
- Inverted the CDP event dependency through a narrow cloneable event-sink port. `debug_cdp_transport.rs` no longer imports the concrete watch gate; the watch layer maps all five typed delivery dispositions, capacity failure irreversibly latches fail-closed health, and the hot path adds no allocation beyond the existing event payload.
- The confirmed native-watch family checkpoint now passes 140/140 tests, including both real Node proofs, generation-one and generation-two `StackTrace`/`Scopes`, transactional FIFO publication, overflow-driven reconnect, logical finish discard semantics and the event-sink dependency boundary.
- Completed the macOS IPv4 **Debug: Attach to Node Process** picker end to end. Descriptor-bound terminal launch and retained workspace authority survive root rename/replacement, while unpublished PTY children are terminated and reaped at every pre-publication failure boundary. Candidate discovery performs bounded `/json/list` exchange only on the exact held loopback socket after kernel PID/reverse-tuple proof, then consumes a second proof of the same opaque connection identity and a fresh full process snapshot.
- Added strict list and start Tauri commands. Listing uses latest-wins transactions, monotonic trust generations, bounded one-shot TTL capabilities and no trust lock across network or `libproc` work. Start atomically consumes the capability, revalidates root, terminal, process and endpoint authority, then proves the separate WebSocket connection and Runtime PID before any Debugger initialization. Process IDs, argv, source paths and WebSocket URLs never cross IPC.
- Added an accessible searchable process picker with keyboard navigation, focus trapping/restoration, loading/empty/error/truncation states, retry and a separate **Attach by port…** action. React receives only locally generated presentation IDs plus bounded label/detail/port; lease capabilities remain in a private controller map, are deleted before the first await and are invalidated on close, refresh, workspace/trust/capability or debug-busy transitions.
- Candidate start now uses the same debug-session acceptance Strategy as ordinary starts: the global start gate, Node breakpoint projection, exception-pause policy, event adoption, exact session ownership and stale-success cleanup are shared. Candidate sessions retain external-process Disconnect semantics but no replayable restart recipe and never fall back to the manual-port gateway.
- Promoted the strict native Node watch path to a public direct launch for the exact `.vscode/launch.json` `--watch` / optional `--watch-preserve-output` subset. The application carries only a closed private intent, uses a dedicated `debug_start_native_node_watch` command and rechecks the exact clean target document before picker admission and again after `preLaunchTask`; raw arguments, environment, shell, npm, nodemon, tsx and a renderer-claimed Node version never cross IPC.
- Enabled the trusted live-filesystem backend on macOS and Linux. The blocking command retains workspace/entrypoint authority, discovers the managed runtime, owns and reaps the process group and preserves one logical debug session across Node target generations. Breakpoints, breakpoint activation, exception pause and `justMyCode` replay before a replacement runs; Stack Trace, Scopes, Variables, Evaluate, Set Variable and Set Expression remain generation/pause-owned. Native watch intentionally retains no generic Restart recipe because file restart belongs to Node.
- Integrated the public watch entry with the existing configuration picker, F5, global debug-start gate and pre/post task lifecycle. Gateway rejection, dirty-after-pre-task, stale workspace/session/picker ownership and accepted stale sessions fail closed; ordinary script, npm, attach, test and compound starts keep their existing paths.
- Split the compound start backend, finish gate, breakpoint-management hook, compound start state and debugger ownership/frame helpers into focused modules. `debug_commands.rs` and `useDebugSession.ts` are now below both new-production size limits, the raw compound-start method is absent from the public debugger contract, and the locked `lib.rs` structural baseline decreased without raising any limit.
- Added diagnostic-only Nette Latte coverage for statically named `{control}` and `{form}` components whose exact presenter/control owner and complete factory absence can be proven. Dynamic or malformed targets, ambiguous/incomplete ownership, unresolved `addComponent` calls and stale cached factory sources fail closed.
- Added independently audited frontend and Rust CAS foundations for future cross-file semantic edits. Immutable single-use leases bind template lifecycle, workspace session/generation, mandatory per-document host epochs, open-owner session/lifecycle/version or closed-owner monotonic revision plus content hash. The Rust foundation enforces epoch tombstones across open/closed/remove/commit transitions, canonical workspace confinement, alias-safe template protection, owner-only full-identity `documentVersions`, duplicate-key rejection and the same 16 MiB UTF-8 wire ceiling as the frontend. Hostile re-entry, replay, mutable edits, malformed backend decisions and A→B→A all fail closed. The Nette factory quick-fix remains deferred until a concrete retained-workspace/open-document adapter connects this foundation directly to the transactional writer.
- Added a separately audited, still-unwired Nette component-factory method planner. It requires exact workspace plus open/closed document identities, parser-clean proofs bound to canonical path and content hash, a complete namespace-scoped owner/ancestor hierarchy and an explicit PHP/Nette syntax capability. Unsupported imports, malformed members, stale ancestors, traits, generic factories and unresolved registrations fail closed; BasePresenter hierarchies, CRLF/indentation and UTF-16 LSP positions are covered. The 256/256 focused checkpoint is clean, but there is deliberately no code-action or apply wiring.
- Rebuilt the production transactional workspace writer around retained root and parent descriptors. Snapshot, stage, revalidation, backup, publish, rollback and recovery use descriptor-relative operations and preserve foreign replacements across root, parent, leaf, cleanup-name and hardlink races. Raw path/count/byte limits precede allocation and I/O; mode-only changes and rollback modes are included. The final 32/32 transaction, 5/5 recovery and 1,756/1,756 Rust-library checkpoints are clean.
- Recovery cleanup deliberately performs no terminal name-based deletion, chmod or truncation because POSIX/macOS cannot atomically unlink a specific opened inode. Exact prior versions are quarantined with their content and mode under persistent per-parent limits of 8,192 entries, 1 GiB and 16,384 scanned entries. Suspicious lookalikes count conservatively and admission fails closed when recovery requires manual inspection. A user-visible inspect/verified-cleanup workflow remains a P2 readiness requirement before this recovery strategy backs a public semantic quick-fix.
- Completed and independently audited the public native-watch command, registration, real intent path and TypeScript application seam. The command performs only cheap closed-intent validation before moving runtime discovery, state lookup, filesystem/trust checks and process/session startup to the blocking pool. Flat camelCase wire fields match the strict gateway, existing `DebugStartResponse` and `debug://event` contracts remain intact, and the real macOS start/reap proof passes.
- Hardened workspace-wide Express route intelligence with bounded four-owner LRU caching, read-hit recency, monotonic async ABA fencing and fixed source/route budgets. Package-aware JSON tuple identities prevent dirty-overlay collisions, empty package labels canonicalize consistently, and malformed delimiter structure removes derived mount authority rather than publishing speculative paths. The focused Express checkpoint passes 65/65 tests.

Current VS Code JavaScript/TypeScript/Node/Express checkpoint:

- Practical editor-core coverage is approximately 80–85% for daily JavaScript/TypeScript/Node/Express work and approximately 85–90% for common single-process Node debugging. This excludes VS Code's extension ecosystem and does not claim parity for child/multi-process, remote/container, universal DAP or Windows native-watch workflows.
- `npm run check`: passing with 0 TypeScript errors
- `npm test`: passing, 1,021/1,021 test files and 15,317/15,317 frontend tests
- Latest full coverage checkpoint: 88.17% statements / 82.22% branches / 93.27% functions / 88.73% lines
- `npm run lint`: passing with zero warnings
- `npm run build`: passing
- `npm audit --json`: zero vulnerabilities
- Focused `set_expression` verification: passing, 26/26 tests, including 7/7 real Node inspector tests
- Focused native-watch verification: 199/199 tests passing, including real generation-one to generation-two replacement, stale-publication rejection, stop/reap, two-phase ABA-safe workspace unregister, fallible supervisor/output pumps and fail-closed root/leaf/intermediate replacement coverage
- Focused semantic workspace edit CAS verification: 19/19 frontend tests, 25/25 Rust atomic tests and 40/40 retained-adapter tests; the final independent audit found no P0/P1 in the private, unreachable foundation
- Focused Nette component-factory planner verification: 256/256 tests; the final independent audit found no P0/P1 and confirmed that no production consumer or apply path exists
- Descriptor-relative transactional writer verification: 32/32 transaction tests, 5/5 recovery tests, repeated root/parent/name/hardlink race stress and a clean independent P0/P1 audit
- Focused B2 components: control proxy 17/17, command worker 11/11, CDP command runtime 3/3 and desired policy 13/13
- Attach-to-process is complete end to end: descriptor-bound terminal authority, kernel-bound HTTP/WebSocket proof, transactional one-shot listing, strict list/start IPC, private capability controller, accessible searchable picker and the shared debug-session acceptance lifecycle. Manual-port attach remains a separate explicit fallback.
- CDP event-sink dependency inversion: complete; typed mapping, irreversible capacity fail-close and source-boundary tests passing
- Rust library tests: passing, 1,782/1,782
- ESLint: 0 warnings; `react-hooks/exhaustive-deps`: 0 findings; Clippy: 0 warnings
- Frontend formatting, changed-file formatting and Rustfmt: passing
- Hotspot review: `useDebugSession.ts` and `debug_commands.rs` remain below the 2,000-line and 10,000-token new-production limits after cohesive Strategy/factory extraction. The baseline was not raised for the reported tracked `lib.rs`, `App.tsx` and `useWorkbenchController.ts` integration growth; `debug_cdp.rs` is materially smaller than its locked baseline.
- `npm run tauri build -- --debug --bundles app`: passing
- `npm run tauri build -- --debug`: passing, producing `.app` and `.dmg` bundles
- Browser smoke test: passing for shell, sidebar tabs, file-outline and reindex UI wiring, bottom-panel Problems/Index/Terminal switching, xterm rendering, terminal non-Tauri fallback, empty states, command palette, language-server runtime subscription wiring, Settings dialog open/save/theme/responsive behavior, Index health responsive behavior, timestamped session-load smoke, light/dark/system theme switching, terminal lazy-load rendering after theme changes, product title/favicon metadata, and non-Tauri development fallback
- `actionlint .github/workflows/macos-release.yml`: passing, 0 findings.
- The PR workflow now gates configuration formatting, a changed-file TypeScript/TSX format ratchet, zero-warning ESLint, type checking, Vitest coverage, the web build, Rust formatting, zero-warning Clippy, and Rust tests in parallel frontend/Rust jobs. `react-hooks/exhaustive-deps` is clean and checked by `npm run lint:exhaustive-deps` with a zero-finding, empty per-file baseline, so any regression fails the gate. Source hotspots have a separate structural-size ratchet (`npm run size:hotspots`): 24 current production/test hotspots are pinned to raw-line and syntax-aware structural-token scores. Template interpolations and Rust lifetimes are counted rather than hidden by string heuristics. New production files may not exceed 2,000 raw lines or 10,000 structural tokens without manual baseline review. `npm run size:hotspots:update` is deliberately monotonic: it can only lower tracked limits or remove deleted files, and refuses tracked growth or new oversized files. Modified legacy TypeScript files that were already unformatted at the PR base remain grandfathered until they are formatted once; new files and previously clean files cannot introduce format drift. The Rust job installs Node dependencies so real TypeScript language-server integration tests run instead of silently skipping. The release workflow keeps its separate packaging-oriented checks.

Known issues:

- Configured process tasks retain a low-severity P2 pathname TOCTOU window between validated executable resolution and operating-system spawn in a trusted writable workspace; a universal descriptor-execution path would break portable shebang and workspace-wrapper behavior.
- The strict npm manifest snapshot has Unix-strength no-follow and inode checks on macOS/Linux. The non-Unix fallback compares regular-file type, size and modification time, so equivalent Windows reparse-point and file-identity hardening remains a P2 portability follow-up before Windows packaging is ready.
- VS Code npm launch import recognizes the exact `npm|npm.cmd (run|run-script) <safe-script>` forms; extra script arguments and npm pre/post lifecycle hooks remain outside the interpreted subset.
- Imported VS Code compounds intentionally support only 2–4 task-free script or safe npm members with `stopAll: true` and at most one compound-level `preLaunchTask`. Attach members, member-level pre/post tasks, `stopAll: false`, compound Restart, Run Without Debugging and arbitrary VS Code/DAP extension configurations remain unsupported.
- Imported `serverReadyAction` intentionally requires all three fields and supports only `openExternally`, a safe literal numeric-port capture and an explicit loopback HTTP(S) URI format. VS Code's arbitrary regex, full-URI capture, browser-debug/startDebugging actions, remote hosts and fallback defaults remain outside the interpreted subset.
- The desktop webview already has the broader `opener:default` capability for existing features such as Markdown Preview. The server-ready application flow revalidates a branded loopback URL through its injected adapter, but this is not a Rust host-enforced boundary against a compromised renderer; replacing all external URL opens with narrow native commands remains a P2 defense-in-depth follow-up.
- Codevo's beyond-VS-Code Node function breakpoints intentionally accept only bounded global
  identifiers or dotted runtime paths. They do not claim arbitrary module-local name matching:
  CommonJS/ES-module lexical functions must be explicitly reachable from a global path, while
  arbitrary expressions, regex matching and anonymous functions remain unsupported. Compound
  sessions intentionally hide and reject function-breakpoint mutation until exact transactional
  fan-out across every child is implemented.
- Debug DMG packaging is unsigned and not notarized. The release plan is documented, but no Developer ID certificate, Apple notarization credentials, or CI signing path is configured yet.
- First release remains a host-runtime build: PHP, PHPactor, Intelephense, Watchman, ripgrep, and shells are not bundled sidecars.
- First release uses manual DMG downloads; in-app auto-update is deferred until Tauri updater keys, endpoints, CI, and UI exist.
- Windows and Linux artifacts are not ready; local macOS cross-target attempts only documented missing targets/toolchains and unsupported bundle targets.
- Native file dialog cannot be fully tested in the regular browser smoke test.

Next parity implementation slice:

1. Prioritize the remaining practical VS Code JavaScript/TypeScript/Node/Express gaps: child/multi-process debug ownership, high-value Variables/Watches actions and the next audit-ranked daily-workflow gaps.
2. Extend watch/debug parity only with separately proven ownership models: Windows native watch, `tsx --watch`, nodemon, npm watch and generic `restart: true` remain unsupported.
3. Resume the deferred Nette quick-fix only after the JS/TS/Node phase; its first safe version must use a synchronous renderer-owned template/owner Monaco mutation lease rather than claiming cross-process disk atomicity.

Release follow-up:

1. Add macOS packaged smoke scripts.
2. Run signed release dry run after Apple secrets are configured.
